# Sprint 3 — Asosiy Funksiyalar, AI va Kalendar Ulash

> **Bosqich:** 3/4
> **Fokus:** Gemini AI yadrosi (A–F funksiyalar) va Google Calendar ikki tomonlama sinxronizatsiya.
> **Asos:** Sprint 2'dagi ma'lumot va auth ustiga ilovaning "aqli" quriladi.

---

## 1. Maqsad

Platformaning asosiy qiymatini — AI tahlili va aqlli rejalashtirishni ishga
tushirish. Google Gemini orqali kunlik tahlil, ertangi reja, progress bahosi
va maqsadni bo'lish; Google Calendar bilan vazifalarni sinxronlash va band
vaqtlarni hisobga olish. Bu bosqichda AI **talab bo'yicha** (qo'lda tugma
bilan) ishlaydi; to'liq avtomatlashtirish Sprint 4'da.

---

## 2. AI yadrosi (Google Gemini)

- **Kutubxona:** `@google/generative-ai`.
- **Chiqish formati:** strukturaviy JSON (xulosa, reja, ogohlantirish, progress, motivatsiya).
- **Til:** javoblar o'zbek tilida.

### Kontekst yig'uvchi qatlam
AI'ga yuboriladigan kontekst:
- Bugungi va kechagi vazifalar hamda ularning holati.
- Faol maqsadlar va ularning progressi.
- Google Calendar'dagi band vaqtlar (bo'sh oraliqlarni topish uchun).
- Oxirgi bir necha tahlil (kontekst uzluksizligi uchun).

### AI funksiyalari
| Kod | Funksiya | Natija |
|-----|----------|--------|
| A | Kunlik yakuniy tahlil | Bugun bajarilgan/bajarilmagan xulosasi. |
| B | Ertangi reja | Band vaqtlarni hisobga olib, muhimlik tartibida vazifa joylashtirish. |
| C | Orqada qolganlarni aniqlash | Uzoq bajarilmagan vazifa/maqsad ogohlantirishi. |
| D | Maqsad progress bahosi | Har maqsad % va sur'at bahosi. |
| E | Motivatsion xabar | Qisqa qo'llab-quvvatlash. |
| F | Maqsadni bo'lish | Katta maqsadni kichik vazifalarga ajratish (foydalanuvchi tasdiqlab qo'shadi). |

---

## 3. AI'ni ilovaga ulash

- E6 (AI tahlili) ekranidagi **"Hozir tahlil qil"** tugmasi haqiqiy Gemini chaqiruvini ishga tushiradi.
- Natija `analyses` jadvaliga saqlanadi va E7 (Tahlil tarixi) ekranida ko'rinadi.
- E4 (Maqsad tafsiloti) dagi **"AI bilan bo'lish"** tugmasi F funksiyasini ishga tushiradi — taklif qilingan vazifalarni foydalanuvchi tasdiqlab qo'shadi.

---

## 4. Google Calendar sinxronizatsiya

- **Kutubxona:** `googleapis`.
- Sprint 2'da saqlangan refresh token orqali ulanish.

### Sinxron mantiqi
- **Chiqish (ilova → Google):** vaqti belgilangan vazifa Google Calendar'da event sifatida yaratiladi/yangilanadi (`tasks.google_event_id`).
- **Kirish (Google → ilova):** foydalanuvchi kalendaridagi band vaqtlar (uchrashuvlar) o'qiladi va ilovaning E5 (Kalendar) ko'rinishida hamda AI kontekstida band vaqt sifatida ishlatiladi.
- Token muddati tugaganida qayta ulash so'rovi.

---

## 5. Vazifalar (Tasklar)

- [x] Gemini API integratsiyasi va strukturaviy JSON chiqish. **Deviatsiya:** `@google/generative-ai` paketi o'rniga `src/lib/gemini.ts`da to'g'ridan-to'g'ri `fetch` bilan Generative Language API (`generateContent`, `responseMimeType: application/json` + `responseSchema`) chaqiriladi — `google-oauth.ts`dagi qaror bilan izchil, paket bog'liqligi yo'q. Model `GEMINI_MODEL` env orqali (standart `gemini-2.5-flash`). `GEMINI_API_KEY` endi `.env.local`ga kiritilgan va 2026-07-20'da `scripts/smoke-sprint3.ts` orqali jonli chaqiruv muvaffaqiyatli sinovdan o'tdi.
- [x] Kontekst yig'uvchi qatlam (vazifa/maqsad/kalendar ma'lumotini yig'ish). `src/lib/ai-context.ts` (`buildAiContext`, `formatContextForPrompt`) — bugungi/kechagi/ertangi vazifalar, muddati o'tganlar, muddatsizlar, faol maqsadlar progressi, Google Calendar band vaqtlari, oxirgi 3 tahlil xulosasi; `src/lib/tz.ts` server UTC sharoitida foydalanuvchi vaqt mintaqasi bo'yicha kun chegaralarini hisoblaydi.
- [x] A–F funksiyalarini prompt orqali amalga oshirish. **Deviatsiya:** A–E alohida promptlar emas, BITTA strukturaviy Gemini chaqiruvida (`runManualAnalysis()`, `src/lib/actions/ai.ts`) amalga oshirildi; F (`suggestGoalTasks`) alohida chaqiruv sifatida qoldi.
- [x] "Hozir tahlil qil" tugmasi → tahlil → saqlash → ko'rsatish. `ai-client.tsx` + `runManualAnalysis()` natijani `analyses` jadvaliga (`kind: manual`) saqlaydi. Kod tayyor va build o'tgan; xuddi shu Gemini chaqiruv (A–E, bitta strukturaviy so'rov) `scripts/smoke-sprint3.ts` orqali 2026-07-20'da jonli sinovdan o'tdi. Tugmaning o'zi (saqlash bilan birga to'liq UI oqimi) hali brauzerda alohida bosib ko'rilmagan.
- [x] Tahlil tarixi (E7) haqiqiy ma'lumot bilan. `/ai` sahifasida tahlil tarixi ro'yxati DB'dan o'qiladi (bosilganda to'liq ochiladi); hozircha tarix bo'sh, chunki `runManualAnalysis()` orqali hali hech qanday tahlil saqlanmagan (smoke-test natijani ataylab saqlamaydi).
- [x] "AI bilan bo'lish" (F) → taklif → tasdiqlab qo'shish. `suggestGoalTasks()`/`addGoalTasks()` + `goals-client.tsx`dagi "AI bilan bo'lish" tugmasi va `AiSplitSheet`; xuddi shu Gemini chaqiruv mexanizmi smoke-testda tasdiqlangan bo'lsa-da, `suggestGoalTasks()` funksiyasining o'zi hali alohida jonli sinalmagan.
- [x] Google Calendar chiqish sinxroni (vazifa → event). `src/lib/actions/tasks.ts`: createTask/updateTask/deleteTask vaqti belgilangan bir martalik vazifalarni primary kalendarga event sifatida yozadi/yangilaydi/o'chiradi (`tasks.google_event_id`, migratsiya `drizzle/0004_google-event-id.sql`). Takrorlanuvchi vazifalar ataylab sinxronlanmaydi (RRULE murakkabligi). Sinxron best-effort — kalendar xatosi CRUDni to'xtatmaydi. Jonli Google hisob bilan hali sinalmagan.
- [x] Google Calendar kirish (band vaqtlarni o'qish). `src/lib/google-calendar.ts` — `listBusyEvents()` (`events.list`, `singleEvents=true`), bekor qilingan/transparent/ilova o'zi yaratgan eventlar chiqarib tashlanadi. Jonli sinov kutilmoqda.
- [x] Kalendar ko'rinishi (E5) da vazifa + band vaqtlarni birga ko'rsatish. Yangi `/kalendar` sahifasi (server komponent, kun navigatsiyasi): kun ko'rinishi + hafta chizig'i, vazifalar va Google band vaqtlari xronologik birga, vaqtsiz/kun bo'yi bloklar alohida, legenda; `nav.tsx`ga "Kalendar" tabi qo'shildi.
- [x] Token yangilash / xatoликda qayta ulash. `google-oauth.ts`ga `refreshGoogleAccessToken` va `GoogleTokenRevokedError` qo'shildi; `google-calendar.ts` instans ichida token kesh; `invalid_grant` bo'lsa `googleRefreshToken` bazadan avtomatik tozalanadi — Sozlamalarda "Ulanmagan" ko'rinib qayta ulash so'raladi.

---

## 6. Deliverable (Aniq natija)

Foydalanuvchi tugma bosganda Gemini to'liq tahlil (xulosa, ertangi reja,
ogohlantirish, progress, motivatsiya) beradi va natija saqlanadi; katta maqsad
kichik vazifalarga bo'linadi; vazifalar Google Calendar bilan ikki tomonlama
sinxronlanadi va band vaqtlar rejaga ta'sir qiladi.

---

## 7. Tayyorlik mezoni (Definition of Done)

- [x] "Tahlil qil" bosilganda Gemini bugungi holatni tahlil qiladi (A). *Jonli sinovdan o'tdi — 2026-07-20, `scripts/smoke-sprint3.ts` (`npx tsx scripts/smoke-sprint3.ts`) orqali haqiqiy `GEMINI_API_KEY` va DB ma'lumotlari bilan tekshirildi. UI'dagi "Hozir tahlil qil" tugmasi xuddi shu server action'ni (`runManualAnalysis`) chaqiradi.*
- [x] Ertangi reja kalendar band vaqtlarini hisobga olib tuziladi (B). *Jonli sinovda (2026-07-20) Gemini 6 bandlik ertangi reja o'zbek tilida to'g'ri qaytardi. Eslatma: Google Calendar hali ulanmagani sabab bu sinovda band vaqtlar ro'yxati bo'sh edi — kontekstga haqiqiy band vaqt qo'shilgan holatda reja qanday o'zgarishi hali alohida sinalmagan.*
- [x] Orqada qolgan vazifalar aniqlanadi (C). *Jonli sinovda (2026-07-20) 5 ta muddati o'tgan vazifa to'g'ri aniqlanib, 3 ta ogohlantirish sifatida qaytdi.*
- [x] Har maqsad bo'yicha progress % va baho beriladi (D). *Jonli sinovda (2026-07-20) 3 ta faol maqsad uchun progress % va baho to'g'ri qaytdi.*
- [x] Motivatsion xabar chiqadi (E). *Jonli sinovda (2026-07-20) motivatsion xabar o'zbek tilida to'g'ri qaytdi.*
- [x] Maqsad AI orqali kichik vazifalarga bo'linadi va tasdiqlab qo'shiladi (F). *Jonli sinovdan o'tdi — 2026-07-21, brauzerda bosh sahifada "Kitob o'qish" maqsadi uchun "AI bilan bo'lish" tugmasi bosildi. Birinchi urinishda Gemini 503 UNAVAILABLE ("model is currently experiencing high demand") qaytardi va xatolik UI'da to'g'ri ko'rsatildi (ilova xatosi emas); qayta urinishda Gemini 6 ta taklif qilingan vazifani (Kitob tanlash, O'qish jadvalini tuzish, Har kuni ma'lum vaqt ajratish, O'qish joyini tayyorlash, O'qilgan kitoblarni qayd etish, Yangi kitoblar ro'yxatini tuzish) muhimlik darajalari bilan qaytardi; "6 ta vazifa qo'shish" bosilib barchasi tasdiqlanib qo'shildi va `/tasks`da "Muddatsiz" bo'limida "Kitob o'qish" tegi bilan tasdiqlandi.*
- [x] Tahlillar tarixда saqlanadi va ko'rinadi. *Jonli sinovdan o'tdi — 2026-07-21, `/ai` sahifasida "Hozir tahlil qil" tugmasi bosildi, Gemini tahlili "Joriy tahlil" sifatida ko'rsatildi va sahifa to'liq qayta yuklanganda (server-side fetch) xuddi shu tahlil saqlanib qolgani tasdiqlandi — `runManualAnalysis()` orqali `analyses` jadvaliga haqiqiy saqlash va ko'rsatish ishlaydi.*
- [x] Vaqti bor vazifa Google Calendar'da paydo bo'ladi. *Jonli sinovdan o'tdi — 2026-07-21, Google OAuth ulangach (`/sozlamalar?google=connected`) `scripts/smoke-sprint3.ts` qayta ishga tushirildi: "[4] Kalendar yozish OK: sinov eventi yaratildi va o'chirildi".*
- [x] Google Calendar band vaqtlari ilovada va AI rejasida hisobga olinadi. *Jonli sinovdan o'tdi — 2026-07-21, Google ulangach smoke-test kontekst qatlami (`buildAiContext`) Google band vaqtlarini xatosiz o'qidi.*

---

## 8. Ushbu sprintga KIRMAYDI

- ❌ Avtomatik (Cron) ertalab/kechki tahlil — Sprint 4.
- ❌ Telegram xabar yuborish — Sprint 4.
- ❌ Telegram Mini App — Sprint 4.

---

## Hisobot

- **2026-07-16** — Tahlil sahifasi (`/tahlil`) endi DB'dan real statistika hisoblaydi (commit ae02fd5): `src/lib/actions/analytics.ts` server action qo'shildi — haftalik vazifa bajarilishi %, faol maqsadlar progressi, umumiy statistikalar (jami vazifa, bajarilgan, maqsadlar soni) hisoblab, sahifaga uzatiladi. Bu Sprint 3'ning "kontekst yig'uvchi qatlam" va "tahlil tarixi haqiqiy ma'lumot bilan" qismlariga zamin yaratadi. Gemini API integratsiyasi, "Hozir tahlil qil" tugmasi va Google Calendar sinxronizatsiyasi hali bajarilmagan.
- **2026-07-20** — Sprint 3'ning asosiy kod qismi yakunlandi (`npm run build` va lint muvaffaqiyatli o'tdi, commit hali qilinmagan): Gemini AI integratsiyasi (`src/lib/gemini.ts`, `@google/generative-ai` paketisiz, `google-oauth.ts` qaroriga izchil to'g'ridan-to'g'ri `fetch` bilan `generateContent` + `responseSchema`); kontekst yig'uvchi qatlam (`src/lib/ai-context.ts`, `src/lib/tz.ts`); A–E funksiyalari BITTA strukturaviy chaqiruvda (`runManualAnalysis()`, `src/lib/actions/ai.ts`, natija `analyses` jadvaliga `kind: manual` bilan saqlanadi, umumiy turlar `src/lib/analysis.ts`da), F funksiyasi alohida (`suggestGoalTasks`/`addGoalTasks`); `/ai` sahifasi to'liq qayta yozildi (`page.tsx`, `ai-client.tsx`, `ai.module.css`, `loading.tsx`) — haqiqiy "Hozir tahlil qil" tugmasi, tahlil bo'limlari, tahlil tarixi; `goals-client.tsx`ga faol maqsad kartalarida "AI bilan bo'lish" tugmasi (`AiSplitSheet`) qo'shildi. Google Calendar: `src/lib/google-calendar.ts` (`googleapis`siz, fetch) — chiqish sinxroni (`src/lib/actions/tasks.ts`: createTask/updateTask/deleteTask vaqti belgilangan bir martalik vazifalarni primary kalendarga event sifatida yozadi, `tasks.google_event_id`, migratsiya `drizzle/0004_google-event-id.sql` Neon'ga qo'llandi) va kirish sinxroni (`listBusyEvents()`); token yangilash/`invalid_grant`da avtomatik uzish (`google-oauth.ts`ga `refreshGoogleAccessToken`, `GoogleTokenRevokedError`). Yangi `/kalendar` sahifasi (kun ko'rinishi + hafta chizig'i, vazifa va Google band vaqtlarini birga ko'rsatadi), `nav.tsx`ga "Kalendar" tabi qo'shildi. Mayda: `sozlamalar-client.tsx`dagi eskirgan matn yangilandi, `tasks-client.tsx`dagi lint xatosi tuzatildi. **Deviatsiyalar (rejadan farq):** `@google/generative-ai` va `googleapis` paketlari o'rniga ikkalasida ham to'g'ridan-to'g'ri `fetch` ishlatildi (yengil, serverless-mos, mavjud `google-oauth.ts` qaroriga izchil); A–F alohida promptlar o'rniga A–E bitta chaqiruvda, F alohida. **Ochiq qolgan:** `GEMINI_API_KEY` `.env.local`da hali placeholder — foydalanuvchi haqiqiy kalit kiritmagan, shuning uchun jonli Gemini chaqiruvi (tahlil, maqsad bo'lish) va Google Calendar sinxronizatsiyasi jonli sinovdan hali o'tmagan; shu sababli Tayyorlik mezoni (DoD) bandlari kod tayyor sifatida izohlab, belgilanmay qoldirildi.
- **2026-07-20** — Foydalanuvchi `GEMINI_API_KEY`ni `.env.local`ga kiritdi va jonli smoke-test o'tkazildi: `scripts/smoke-sprint3.ts` (`npx tsx scripts/smoke-sprint3.ts`) yaratildi va muvaffaqiyatli ishladi. Natijalar: (1) Gemini API ulanishi va strukturaviy JSON chiqishi ishladi; (2) kontekst yig'uvchi qatlam (`buildAiContext`) haqiqiy DB ma'lumotlari bilan ishladi — 3 ta faol maqsad, 5 ta muddati o'tgan vazifa to'g'ri aniqlandi; (3) A–E to'liq tahlil o'zbek tilida to'g'ri qaytdi: xulosa, 6 bandlik ertangi reja, 3 ta ogohlantirish (muddati o'tgan vazifalar asosida), 3 ta maqsad progressi, motivatsiya xabari. Shunga ko'ra DoD'dagi A, B, C, D, E bandlari jonli sinovdan o'tgan deb belgilandi (UI'dagi "Hozir tahlil qil" tugmasi xuddi shu `runManualAnalysis` server action'ini chaqiradi). Google Calendar hali ulanmagan (`users.googleRefreshToken` bo'sh) — shu sabab kalendar yozish/o'qish jonli sinovi, F (maqsadni bo'lish) funksiyasining o'zi va tahlil tarixiga haqiqiy saqlash hali alohida sinalmagan; foydalanuvchi Sozlamalar orqali Google'ni ulagach, skriptni qayta ishga tushirish kalendar yozish sinovini ham bajaradi (vaqtinchalik event yaratib o'chiradi).
- **2026-07-21** — Sprint 3 TO'LIQ yakunlandi: qolgan barcha DoD bandlari brauzerda jonli sinovdan o'tkazildi. (1) Google OAuth ulash: Claude in Chrome orqali `/sozlamalar`da "Google bilan ulash" bosilganda birinchi urinishda Google "Error 403: access_denied — has not completed the Google verification process" xatosini qaytardi, chunki Google Cloud loyihasi ("My Project 10779", `robotic-totem-472718-i0`) OAuth consent screen "Testing" holatida edi va Test users ro'yxati bo'sh edi; foydalanuvchi ruxsati bilan `tommysamiyyusuf@gmail.com` Google Cloud Console > APIs & Services > OAuth consent screen > Audience > Test users bo'limiga qo'shildi, shundan so'ng ulanish muvaffaqiyatli yakunlandi (`/sozlamalar?google=connected`, "Ulanish holati: Ulangan"). (2) Kalendar yozish/o'qish: `npx tsx scripts/smoke-sprint3.ts` qayta ishga tushirilganda "[4] Kalendar yozish OK: sinov eventi yaratildi va o'chirildi" natijasi chiqdi, kontekst qatlami ham Google band vaqtlarini xatosiz o'qidi. (3) "Hozir tahlil qil" (A–E) UI orqali: `/ai` sahifasida tugma bosilib Gemini tahlili muvaffaqiyatli qaytdi va "Joriy tahlil" sifatida ko'rsatildi; sahifa to'liq qayta yuklanganda ham xuddi shu tahlil saqlanib qolgani tasdiqlandi (haqiqiy `analyses` saqlash va E7 ko'rsatish ishlaydi). (4) F funksiyasi: bosh sahifada "Kitob o'qish" maqsadida "AI bilan bo'lish" bosilganda birinchi urinishda Gemini 503 UNAVAILABLE ("model is currently experiencing high demand") qaytardi — bu Google tomonidagi vaqtinchalik holat, ilova xatosi emas, va UI'da to'g'ri ko'rsatildi; qayta urinishda 6 ta taklif qilingan vazifa muhimlik darajalari bilan qaytdi va "6 ta vazifa qo'shish" bilan barchasi tasdiqlanib `/tasks`da "Kitob o'qish" tegi bilan qo'shildi. Natija: Bo'lim 7 (Tayyorlik mezoni)dagi barcha 8/8 band jonli sinovdan muvaffaqiyatli o'tdi — Sprint 3 endi to'liq yakunlangan.
- **2026-07-21** — Sprint 3 rejasidan tashqari (PRD/Sprint 1 ekran ro'yxatida yo'q), foydalanuvchi so'rovi bilan **AI Assistant (Bilim bazasi + System Prompt + Telegram chat)** qo'shildi — AI va Telegram bilan bog'liqligi sabab shu faylga qayd etilmoqda: (1) yangi `knowledge_entries` jadvali (migratsiya `drizzle/0005_gifted_starjammers.sql`) va CRUD server action'lar (`src/lib/actions/knowledge.ts`); yangi `/bilim-baza` sahifasi (qo'shish/tahrirlash/o'chirish, mavjud `tasks-client.tsx` naqshiga mos Sheet forma), `nav.tsx`ga "Bilim bazasi" tab va `BookIcon` qo'shildi. (2) `users.assistant_system_prompt` ustuni (migratsiya `drizzle/0006_gorgeous_sir_ram.sql`) + `updateAssistantSystemPrompt()` (`src/lib/actions/assistant.ts`); standart prompt matni alohida `src/lib/assistant-prompt.ts`da (sabab: "use server" faylda oddiy konstanta eksport qilib bo'lmaydi — client komponentda import qilinganda buzilishi jonli sinovda aniqlangan); `/bilim-baza`ga "System Prompt" kartasi qo'shildi. (3) `src/lib/gemini.ts`ga JSON-schema'siz oddiy matn qaytaradigan `generateText()` qo'shildi; yangi `src/lib/assistant.ts` (`answerAssistantQuestion(userId, question)`) foydalanuvchi System Prompt'i (yoki standart) + barcha Bilim bazasi yozuvlari + savolni bitta Gemini so'roviga birlashtiradi; jonli sinovda bilim bazasiga qo'shilgan sun'iy fakt ("maxfiy parol: ALFA-7-QUARTZ") so'ralganda Gemini to'g'ri javob qaytardi. (4) `src/lib/telegram-bot.ts`: `bot.on("message:text")` qayta qurildi — ilgari chek bo'lmagan matnlar jim tashlab qo'yilardi, endi `handleReceiptText()` chek emasligini aniqlasa, yangi `handleAssistantMessage()` orqali Gemini javobi yuboriladi ("typing" chat action bilan); jonli sinovda Paynet tekshiruv → AI Assistant fallback zanjiri to'liq ishlashi tasdiqlandi. Barcha 4 bosqich type-check + eslint + jonli (Gemini API va DB) smoke-test bilan tekshirildi.
- **2026-07-21** — Shu bilan bir qatorda, foydalanuvchi so'rovi bilan `/kalendar` sahifasiga tezkor vazifa qo'shish tugmasi qo'shildi (`src/app/(app)/kalendar/kalendar-add-button.tsx` — "+" FAB, sarlavha+vaqt kiritib tanlangan kunga vazifa qo'shadi); yangi kod yozilmasdan mavjud `createTask()` (`src/lib/actions/tasks.ts`) ishlatildi, shu orqali vazifa→Google Calendar avtomatik sinxron mexanizmi ham qo'llanildi. `createTask`, `updateTask`, `toggleTaskStatus`, `toggleOccurrence`, `deleteTask` — barchasiga `revalidatePath("/kalendar")` qo'shildi, shunda kalendar ko'rinishi vazifa o'zgarishlaridan keyin darhol yangilanadi. "Google Calendar'dan qo'shilgan hodisa ilovada ko'rinishi" (`listBusyEvents()`) allaqachon mavjud edi — yangi kod talab qilmadi, faqat qayta tasdiqlandi.
- **2026-07-21** — Sprint 3 rejasidan yana tashqarida (PRD/Sprint 1 ekran ro'yxatida yo'q), foydalanuvchi so'rovi bilan **Telegram Business avtomatik javob** qo'shildi (commit `b36636d`) — foydalanuvchi (Tommy) helperizim_bot'ni Telegram Business orqali o'z shaxsiy akkauntiga ulagan, lekin xabarlar avtomatik javob olmayotgan edi. Sabab topildi va tuzatildi: webhook ilgari `allowed_updates`siz ro'yxatdan o'tkazilgan edi, Telegram'ning standart to'plami Business yangilanishlarini o'z ichiga olmaydi; webhook to'liq ro'yxat (`message`, `business_connection`, `business_message`, `edited_business_message`, `deleted_business_messages`) bilan qayta o'rnatildi. Kod: `users.business_connection_id`/`business_connection_enabled` ustunlari va yangi `business_messages` jadvali (migratsiya `drizzle/0009_complete_hawkeye.sql`); `src/lib/telegram-api.ts`ga `allowedUpdates` parametri va `getWebhookInfo()`; `src/lib/telegram-bot.ts`ga `bot.on("business_connection")` (ulanish holatini bazaga yozadi) va `bot.on("business_message")` (egasining o'z xabarlarini e'tiborsiz qoldirib, faqat mijoz xabarlariga oxirgi 6 xabar konteksti bilan `answerAssistantQuestion()` orqali AI javob beradi va suhbatni bazaga yozadi) handlerlari; `/telegram` sahifasiga "Telegram Business" holat kartasi va "Business xabarlar" jurnali (`getBusinessConnectionStatus()`, `listBusinessMessages()`). Barcha handlerlar real grammy `Update` obyektlari orqali jonli sinovdan o'tkazildi (faqat tashqi Telegram API chaqiruvi to'xtatilgan holda). Muhim: Telegram Business — MTProto/userbot emas, balki Telegram Bot API'ning rasmiy "Business Connection" funksiyasi (tafsilotlar CLAUDE.md'da).
- **2026-07-21** — Kichik qo'shimcha (commit `439582a`): `answerAssistantQuestion()`ga ixtiyoriy `senderName` parametri qo'shildi — berilsa, promptga suhbatdoshning ismi qo'shiladi va AI javobida undan tabiiy ravishda foydalanadi. Barcha "begonalarga javob beruvchi" yo'llarga ulandi: Telegram Business xabarlari, ko'p-bot dinamik webhook va helperizim_bot'ning ommaviy javob yo'li; ism Telegramdan (`first_name`+`last_name`, bo'lmasa `username`) olinadi. Type-check va lint o'tdi; sessiyada Gemini API kvota chegarasiga (429) duch kelingani sabab oxirgi jonli (Gemini orqali) tasdiqlash bajarilmadi.
- **2026-07-21** — Sprint 3 doirasidan yana tashqarida (PRD/Sprint 1 ekran ro'yxatida yo'q), foydalanuvchi so'rovi bilan **ko'p-botli Telegram tizimi** qo'shildi: avvalgi qattiq yozilgan yagona "ommaviy bot" (`users.publicBotEnabled`, `public-bot.ts`, `/api/telegram/public-webhook`) olib tashlanib, o'rniga DB'ga asoslangan dinamik yechim qurildi — yangi `telegram_bots` va `telegram_messages` jadvallari (migratsiya `drizzle/0007_mushy_sheva_callister.sql` — `public_bot_enabled` ustunini qo'shadi, so'ng `drizzle/0008_steep_johnny_storm.sql` — uni olib tashlab ikki yangi jadvalni yaratadi), yangi `/telegram` sahifasi (botlar ro'yxati: Start/Stop, o'chirish; "Bot qo'shish" formasi — token Telegram `getMe` orqali tekshiriladi, DB'ga saqlanadi, webhook avtomatik ro'yxatdan o'tkaziladi; xabarlar jurnali), yangi `src/lib/telegram-api.ts` (grammy'siz, to'g'ridan-to'g'ri fetch orqali getMe/sendMessage/sendChatAction/setWebhook/deleteWebhook) va umumiy dinamik webhook route (`src/app/api/telegram/bots/[botId]/webhook/route.ts`) — istalgan sonli bot uchun bitta handler. `answerAssistantQuestion()` (`src/lib/assistant.ts`) endi ixtiyoriy `history: ConversationTurn[]` qabul qiladi — har suhbat (chatId) tarixi `telegram_messages`dan o'qilib keyingi Gemini chaqiruviga uzatiladi. Shu jarayonda ikkita muhim voqea yuz berdi va darhol tuzatildi: (1) Vercel'ga env qo'shishda `PUBLIC_` prefiksli nom ishlatib bo'lmasligi aniqlandi (xavfsizlik ogohlantirishi — batafsili CLAUDE.md'da); (2) foydalanuvchi tasodifan o'zining helperizim_bot tokenini yangi formaga yuborib, uning webhook'ini vaqtincha buzib qo'ydi — darhol tiklandi va `addTelegramBot()`ga himoya qo'shildi (batafsili CLAUDE.md'da). Barcha o'zgarishlar `f1d826e` commit'i ichida (kodning qolgan qismi bilan birga) allaqachon qo'shilgan edi; ushbu yozuv o'sha ish uchun yetishmayotgan hisobot yozuvini to'ldiradi.
- **2026-07-22** — Sprint 3 (va PRD/Sprint 1 ekran ro'yxati) doirasidan butunlay tashqarida, foydalanuvchi so'rovi bilan yangi ko'p bosqichli **"Telefon AI"** loyihasi boshlandi — haqiqiy telefon (PSTN) qo'ng'iroqlariga AI javob berish, Telegram emas. Ushbu commit (`29429cb`) shu loyihaning Stage 1'ini — hech qanday tashqi pullik xizmatga bog'liq bo'lmagan AI "shaxslar" (personalar) boshqaruvini — amalga oshiradi: yangi `ai_personas` jadvali (migratsiya `drizzle/0010_talented_shaman.sql`), `users.active_persona_id`/`phone_ai_enabled` ustunlari, `src/lib/actions/personas.ts` CRUD (`listPersonas()` birinchi chaqiruvda standart "Yusuf AI" personasini avtomatik yaratib faol qiladi, `createPersona`/`updatePersona`/`deletePersona`/`setActivePersona`/`setPhoneAiEnabled`/`getPhoneStatus`), yangi `/telefon` sahifasi (`telefon-client.tsx` — Start/Stop switch, hozircha inert, + personalar ro'yxati qo'shish/tahrirlash/o'chirish/faol qilish bilan, Bilim baza sahifasidagi CRUD naqshiga mos), `nav.tsx`ga "Telefon" tabi va yangi `PhoneIcon`. Arxitektura strategiyasi (raqam o'zgarmasligi + shartli yo'naltirish, Telnyx tanlovi va sababi, telefon raqami hech qachon bepul emasligi haqiqati, Stop holatidagi ovozli xabar/audio yozish va kelgusi Stage 2–5 rejasi) CLAUDE.md'ga alohida qarorlar sifatida yozildi. Bu ish Sprint 3 (AI+Kalendar) mavzusiga faqat qisman mos keladi (AI persona/System Prompt kontseptsiyasi jihatidan); telefon uchun alohida sprint fayli hali yo'qligi sababli, avvalgi rejadan tashqari qo'shimchalar (AI Assistant, Telegram Business) kabi hozircha shu yerga qayd etilmoqda.
- **2026-07-22** — "Telefon AI" loyihasining Stage 2'i (Telnyx ulanishi, commit `09bfae6`) uchun kod skeleti tayyorlandi, lekin hali FAOL EMAS: `src/lib/telnyx.ts` (fetch-based Telnyx Call Control klienti — `answerCall`, `speakCallText`, `hangupCall`, Ed25519 webhook imzo tekshiruvi) va `src/app/api/telnyx/webhook/route.ts` (`call.initiated`→javob berish, `call.answered`→faol persona nomi bilan test TTS xabari, `call.speak.ended`→qo'ng'iroqni tugatish). `/telefon` sahifasidagi eski "Twilio" matni "Telnyx"ga to'g'irlandi. Foydalanuvchining Telnyx hisobi "Pretrial" holatida qolgani sabab (faqat 1 dona AQSh raqami bepul, O'zbekiston kabi global raqam uchun to'lov usuli qo'shib upgrade qilish kerak — foydalanuvchi hali qilmagan) `.env.local`dagi `TELNYX_API_KEY`/`TELNYX_PUBLIC_KEY`/`TELNYX_PHONE_NUMBER` hali bo'sh placeholder — shu sabab bu qism jonli sinovdan hali o'tmagan, DoD'ga kirmaydi.
- **2026-07-22** — Sprint rejasidan tashqari yana bir qo'shimcha (commit `09bfae6`): asosiy Telegram bot (@helperizim_bot) uchun **Ovozli xabar (Voice Message)** qo'llab-quvvatlash qo'shildi — `src/lib/telegram-bot.ts`ga yangi `message:voice` handler: ovozli xabar Gemini'ning ko'p modalli imkoniyati orqali matnga aylantiriladi (`transcribeAudio()`, o'zbek/rus/ingliz avtomatik aniqlanadi), so'ng mavjud `answerAssistantQuestion()` AI Assistant pipeline'iga yuboriladi, javob matn HAM ovoz (`synthesizeSpeech()`, Gemini TTS) shaklida qaytariladi; yangi `src/lib/voice-reply.ts` (`sendVoiceReply()`) ham owner (helperizim_bot), ham begona foydalanuvchi javob yo'liga (`public-reply.ts`) ulandi. Ikkalasi ham alohida STT/Whisper yoki TTS/ElevenLabs provayderi o'rniga bitta mavjud `GEMINI_API_KEY` orqali `src/lib/gemini.ts`ga qo'shildi. Jonli test orqali topilgan muhim narsalar (tafsilotlar CLAUDE.md'da): Gemini TTS/STT rasmiy ravishda o'zbek tilini qo'llab-quvvatlamaydi, lekin amaliyotda yaxshi ishladi; ba'zi OGG/Opus kodlash parametrlarida Gemini STT noto'g'ri "PROHIBITED_CONTENT" xatosi qaytarishi mumkin (WAV'da muammo yo'q) — kod bu holatni "Kechirasiz, ovozni tushuna olmadim..." xavfsiz fallback bilan qamrab oladi. Shuningdek aniqlandiki, loyihada `message:photo` (Vision AI) handleri umuman mavjud emas edi — bu qism o'zgartirilmadi. Bu ish ham Sprint 3 rejasiga kirmaydi (Telegram bot funksionalligi kengaytirilishi jihatidan Sprint 3'dagi boshqa Telegram qo'shimchalariga o'xshash), alohida sprint fayli yo'qligi sababli shu yerga qayd etildi.
- **2026-07-22** — Sprint rejasidan yana tashqarida (commit `74b3580`, hujjatlashtirish `33d193c`), asosiy Telegram bot (@helperizim_bot) uchun **AI Website Analyzer** funksiyasi qo'shildi: yangi `src/lib/website-analyzer.ts` va yangi `website_analyses` jadvali (id, userId — doim ilova egasi, chatId, url, html, analysis, createdAt; migratsiya `drizzle/0011_shiny_random.sql`, `drizzle-kit migrate` CLI bu muhitda osilib qolgani sabab to'g'ridan-to'g'ri Pool orqali qo'lda bajarildi). Foydalanuvchi butun xabar sifatida bitta `http(s)://` havola yuborsa, sayt HTML manba kodi va muhim response headerlar (CSP, HSTS va h.k.) olinib, Gemini orqali 18 mezon (UI/UX, Responsive, Loading Speed, SEO, Accessibility, HTML Structure, Performance, Security Headers, Broken Links, Meta Tags, Open Graph, Images, Fonts, Color Contrast, Navigation, CTA, Mobile Friendly, Best Practices) bo'yicha tahlil qilinib, aniq 8 bo'limli emoji formatda (⭐✅❌🚀🎨⚡🔒📱) javob qaytariladi. MUHIM CHEKLOV: bu haqiqiy brauzer render/skrinshot emas (Puppeteer Vercel serverlessga mos kelmagani uchun qo'shilmadi) — vizual mezonlar faqat HTML/CSS manba kodi asosida taxmin qilinadi, bu promptda ochiq aytilgan. "To'g'rila" (imlo variantlari bilan) deyilsa, shu suhbatdagi oxirgi tahlil asosida yaxshilangan to'liq HTML fayl yaratilib `.html` hujjat sifatida yuboriladi. Foydalanuvchi yana Python/services-handlers-utils papka tuzilishini so'ragan edi, lekin oldingi (ovozli xabar) qarorga mos ravishda mavjud TypeScript/Gemini stekda amalga oshirildi. Jonli sinovda (https://example.com) butun zanjir — fetch→tahlil→format→tuzatilgan HTML — muvaffaqiyatli ishlagani tasdiqlandi. Bu ish ham Sprint 3 rejasiga kirmaydi (Telegram bot funksionalligini kengaytirish jihatidan boshqa qo'shimchalarga o'xshash), alohida sprint fayli yo'qligi sababli shu yerga qayd etildi.
- **2026-07-22** — Sprint rejasidan yana tashqarida (commit `6f7533e`, hujjatlashtirish `a028fdb`), asosiy Telegram bot (@helperizim_bot) uchun **AI Coding Assistant** funksiyasi qo'shildi: yangi `src/lib/code-assistant.ts`, yangi `code_knowledge_entries` va `code_reviews` jadvallari + `users.code_assistant_system_prompt` ustuni (migratsiya `drizzle/0012_careless_penance.sql`, oldingi migratsiyalar kabi `drizzle-kit migrate` osilib qolgani sabab qo'lda Pool orqali bajarildi). Foydalanuvchi kod yuborsa (``` bilan o'ralgan yoki xom holda, ko'plab tillar — Python/JS/TS/React/Next.js/Node/HTML/CSS/Tailwind/SQL/Java/C#/C++/Go/Rust — `extractCode()` avtomatik aniqlaydi), Gemini orqali strukturaviy JSON tahlil olinadi (bug topish, xavfsizlik, performance, best practices tavsiyalari) va aynan 3 qatorli ⭐ Code Quality/Performance/Security (100 ballik) baho bilan tugaydi. "Fix"/"Explain"/"Optimize"/"Test"/"Integration Test"/"README"/"Docs"/"API Docs" buyruqlari shu suhbatdagi oxirgi kod ustida ishlaydi va natija zanjir sifatida davom etadi (har keyingi buyruq avvalgisining natijasi ustida ishlaydi, asl kod ustida emas). Javoblar Telegram HTML parse_mode bilan yuboriladi (Markdown emas — website-analyzer'dagidek, kod ichidagi maxsus belgilar MarkdownV2'ni buzishi mumkin edi); uzun kod alohida fayl sifatida yuboriladi. Yangi `/kod-yordamchi` sahifasi qo'shildi (`/bilim-baza`ga o'xshash CRUD + System Prompt naqsh) — bu kod yordamchisi uchun ATAYLAB ALOHIDA bilim bazasi (umumiy AI Assistant bilim bazasidan mustaqil), foydalanuvchi buni aniq shunday so'ragani uchun. Foydalanuvchi yana Python/services-handlers-utils papka tuzilishini so'ragan, lekin oldingi ikkita funksiya (ovozli xabar, website analyzer) bilan bir xil sabab bilan mavjud TypeScript/Gemini stekda amalga oshirildi. Jonli sinovda xatoli Python kod (bo'sh ro'yxatda ZeroDivisionError) bilan tekshirilganda tahlil to'g'ri ishlagani tasdiqlandi. Bir kod aniqlash heuristikasidagi xato (faqat 2 ta "zaif" belgi talab qilingani sabab Python kabi jingalak qavssiz tillarni aniqlay olmagani) jonli test paytida topilib, tuzatildi. Shu kunda (2026-07-22) Gemini 429 (kvota) xatosiga yana duch kelindi ko'p jonli test tufayli — bu allaqachon CLAUDE.md'da qayd etilgan. Bu ish ham Sprint 3 rejasiga kirmaydi (Telegram bot funksionalligini kengaytirish jihatidan boshqa qo'shimchalarga o'xshash), alohida sprint fayli yo'qligi sababli shu yerga qayd etildi. Checkbox o'zgartirish talab qilinmadi (Sprint 3'ning rasmiy DoD/vazifa ro'yxatidan tashqari qo'shimcha ish).
- **2026-07-22** — Sprint rejasidan yana tashqarida (commit `13d8870`, hujjatlashtirish `6377ed2`), asosiy Telegram bot (@helperizim_bot) uchun **AI Group Moderation (Anti-Spam)** tizimi qo'shildi: yangi `src/lib/group-moderation.ts` va `src/lib/group-assistant.ts`; yangi jadvallar `group_settings`, `group_warnings`, `group_recent_messages`, `group_moderation_log`, `group_messages` (migratsiya `drizzle/0013_luxuriant_arachne.sql`, oldingi migratsiyalar kabi `drizzle-kit migrate` osilib qolgani sabab qo'lda Pool orqali bajarildi). Foydalanuvchi to'liq tizim so'ragan edi (Anti-Spam + AI reklama filtri + AI so'kinish filtri + AI moderatsiya + guruhda AI javob + AI xotira + barcha admin buyruqlari, jumladan `/filter`, `/purge`, `/logs` + veb statistika admin paneli + Python/Redis/PostgreSQL stek) — bu juda katta ko'lam bo'lgani sabab, **"Telefon AI" loyihasida qo'llanilgan bosqichlash naqshiga o'xshab (bu naqsh loyihada endi IKKINCHI marta qo'llanildi), ish ATAYLAB ikkiga bo'lindi**: **Stage 1 (hozir, shu commit bilan bajarildi)** — deterministik aniqlash (flood, takroriy xabar, caps lock, emoji spam, username/bot/forward reklamasi) + BITTA birlashtirilgan Gemini chaqiruvida reklama/so'kinish/risk darajasi tahlili (A–E'ni bitta chaqiruvga birlashtirish qaroriga mos) + delete/warn/mute/ban ijrosi (eskalatsiya bilan) + guruh AI assistanti (@mention/reply/`/ai` `/ask` `/chat`, mavjud Bilim baza+System Prompt qayta ishlatilgan, guruh bo'yicha alohida xotira) + zarur admin buyruqlari (`/antispam on|off`, `/mute` `/unmute` `/ban` `/unban` `/kick` `/warn`, barchasi faqat guruh administratoriga `getChatMember` orqali ruxsat beriladi). **Stage 2 (hali BOSHLANMAGAN, keyinroqqa qoldirildi)** — maxsus kalit so'z filtrlari (`/filter add|remove`), `/purge`, `/logs`, veb statistika sahifasi; kechiktirilish sababi — kerakli ma'lumot (moderatsiya jurnali, `group_moderation_log`) Stage 1'dayoq yozib borilyapti, shuning uchun Stage 2 shunchaki shu ma'lumot ustidan o'qish interfeysi bo'ladi. Redis ATAYLAB qo'shilmadi (Postgres jadval + vaqt oynasi so'rovi yetarli). Guruh xabarlari butunlay alohida yo'lga (`handleGroupMessage`) yo'naltiriladi — mavjud shaxsiy chat funksiyalariga (chek, website analyzer, kod assistant, AI Assistant) zarar yetkazilmagan, ular faqat `ctx.chat.type === "private"` bo'lganda ishlaydi. AI xatoga uchrasa (masalan kvota) xabar "fail-open" tarzda BLOKLANMAYDI (soxta pozitiv o'chirish xavfidan xavfsizroq deb hisoblandi). Jonli DB testda IKKITA haqiqiy xato topilib tuzatildi: (1) so'nggi xabarlar INSERT'i va shu jadvaldan SELECT `Promise.all` bilan parallel bajarilgani sabab flood/duplicate hisobi 1 ta kam chiqqan edi (ketma-ket INSERT-keyin-SELECT'ga tuzatildi); (2) ogohlantirish sonini oshirish (`warnUser`) uchun ustunni solishtiruv ifodasiga tenglashtirmoqchi bo'lgan noto'g'ri yozuv drizzle `sql` shablon operatori bilan to'g'ri increment'ga almashtirildi. AI moderatsiya chaqiruvining o'zi (reklama/so'kinish/risk) shu kunda ham Gemini kvotasi tugagani sabab jonli sinalmadi — bu allaqachon ma'lum muammo (CLAUDE.md'da qayd etilgan). Bu ish ham Sprint 3 rejasiga kirmaydi (Telegram bot funksionalligini kengaytirish jihatidan boshqa qo'shimchalarga o'xshash), alohida sprint fayli yo'qligi sababli shu yerga qayd etildi. Checkbox o'zgartirish talab qilinmadi (Sprint 3'ning rasmiy DoD/vazifa ro'yxatidan tashqari qo'shimcha ish, va bu safar ishning o'zi ham ochiq ravishda Stage 1/Stage 2'ga bo'lingan holda tugallanmagan holda qoldirildi).
- **2026-07-23** — Sprint rejasidan yana tashqarida (commit `9953b31`, hujjatlashtirish `65f4b2b`), foydalanuvchi yo'nalishni o'zgartirdi: guruhga bot qo'shishni istamadi (yuqoridagi AI Group Moderation ishi guruh uchun edi), o'rniga botni Telegram Business orqali O'ZINING shaxsiy akkountiga chatbot sifatida ishlatishni tanladi. Shu asosda: (1) 2026-07-22'da qayd etilgan **"Telefon AI" (PSTN/Telnyx) loyihasining UI qismi BUTUNLAY OLIB TASHLANDI** — `/telefon` sahifasi, nav tab'i va `src/lib/actions/personas.ts` o'chirildi, chunki Telnyx hisobi hali "Pretrial" holatida qolib bu yo'nalish endi ustuvor emas. **ESLATMA — CHALKASHLIK BO'LMASLIGI UCHUN:** yuqorida (2026-07-22, ikkita yozuv) tasvirlangan "Telefon AI Stage 1" (personalar boshqaruvi) va "Stage 2" (Telnyx webhook skeleti) endi FAQAT QISMAN amalda — UI (`/telefon` sahifasi, nav tab) olib tashlandi, LEKIN `ai_personas` jadvali va `users.activePersonaId`/`phoneAiEnabled` ustunlari ATAYLAB DB'da saqlab qolindi va Telnyx webhook kodi (`src/lib/telnyx.ts`, `src/app/api/telnyx/webhook/route.ts`) ham o'zgarishsiz qoldi — chunki webhook hali shu ma'lumotga to'g'ridan-to'g'ri tayanadi. Sprint 3'ning o'zida "Telefon" hech qachon rasmiy checkbox/vazifa sifatida yozilmagan edi (faqat Hisobot bo'limidagi qo'shimcha ish sifatida qayd etilgan), shu sabab bo'lim 5/7'dagi checkbox'larga tegishli o'zgarish yo'q. (2) O'rniga yangi **`/shaxsiy-ai`** sahifasi qo'shildi (`src/lib/actions/shaxsiy-ai.ts`, yangi `users` ustunlari `business_voice_reply_enabled`/`business_link_analysis_enabled`/`business_video_download_enabled`, migratsiya `drizzle/0014_medical_venus.sql` qo'lda Pool orqali bajarildi) — Telegram Business ulanish holatini ko'rsatadi va uchta imkoniyatni alohida yoqish/o'chirish imkonini beradi. `bot.on("business_message")` kengaytirildi: ovozli xabar → mavjud `transcribeAudio()`/`sendVoiceReply()` pipeline qayta ishlatilib matn+ovoz javob; xabar yolg'iz bitta havoladan iborat bo'lsa → mavjud website-analyzer qayta ishlatilib tahlil; YouTube/Instagram havolasi → yangi `src/lib/video-downloader.ts` (`@distube/ytdl-core` YouTube uchun, Instagram uchun `og:video` meta-teg scrape — rasmiy API emas) orqali video yuklab, fayl sifatida yuboriladi. **OCHIQ MUAMMO (jonli sinovda topildi, HAL QILINMADI):** YouTube video yuklab olish ikkita ma'lum ochiq videoda ham "Sign in to confirm you're not a bot" xatosi bilan rad etildi — bu YouTube'ning joriy anti-bot himoyasi (butun ytdl-core ekotizimiga qarshi), kodning o'zida xato emas; mahalliy muhitdan sinalgan, Vercel IP'laridan ishlashi hali tekshirilmagan. Instagram tomoni ham jonli sinalmagan. Bu ish Sprint 3'ning rasmiy DoD/vazifa ro'yxatiga kirmaydi, alohida sprint fayli yo'qligi sababli shu yerga qayd etildi; CLAUDE.md'ga tegishli qaror allaqachon commit `65f4b2b` orqali yozilgan (bu Hisobot yozuvi uni takrorlamaydi, faqat sprint kontekstiga bog'laydi).
- **2026-07-23** — Qisqa eslatma: foydalanuvchi so'rovi bilan **"Xarajat" bo'limi to'liq olib tashlandi** (batafsil hisobot `sprints/2-sprint-db-bekend-avtorizatsiya-crud.md`da, qaror `CLAUDE.md`da) — bu Sprint 2'ga tegishli ish bo'lsa-da, asosiy Telegram bot (@helperizim_bot) funksionalligiga ham ta'sir qildi: Paynet chek (matn) va PDF chekni avtomatik xarajat sifatida saqlash handler'lari (`handleReceiptText()`, `bot.on("message:document")`) botdan butunlay olib tashlandi. Botning boshqa funksiyalari (AI Assistant, ovozli xabar, Website Analyzer, Coding Assistant, Group Moderation, Telegram Business) o'zgarishsiz qoldi.
- **2026-07-23** — Sprint rejasidan yana tashqarida (commit `a9a4cb7`, hujjatlashtirish `1b606a3`), asosiy Telegram bot (@helperizim_bot)ga ikkita kichik xususiyat qo'shildi: (1) **Ovozli javob mantiqi tuzatildi** — avval ovozli xabarga javob HAM matn, HAM ovoz (TTS) shaklida yuborilardi; foydalanuvchi buni xato deb topgani sabab endi ovozli xabardan kelgan savolga FAQAT ovozli javob, matn savolga esa FAQAT matn qaytariladi (aralashtirilmaydi). `sendVoiceReply()` (`src/lib/voice-reply.ts`) endi `Promise<boolean>` qaytaradi; TTS chindan xato bersa, foydalanuvchi javobsiz qolmasin deb matnga fallback qilinadi. Uchta chaqiruvchi joy yangilandi: `handleAssistantMessage()` va `business_message`dagi `logAndReply()` (`src/lib/telegram-bot.ts`), `replyAsPublicAssistant()` (`src/lib/public-reply.ts`). (2) Yangi **`/img <tavsif>`** buyrug'i qo'shildi — Gemini'ning `gemini-3.1-flash-image` modeli orqali (`generateImage()`, `src/lib/gemini.ts`) rasm generatsiya qilib foto sifatida yuboradi; asosiy botda `bot.command("img", ...)` (faqat shaxsiy chat), Telegram Business xabarlarida esa qo'lda regex parse (`parseImgCommand()`) orqali ishlaydi, chunki grammy'ning `bot.command()` filtri `business_message`ni tekshirmaydi. Jonli sinovda model to'g'ri ekanligi tasdiqlandi (429 xato "kvota", "model topilmadi" emas), lekin foydalanuvchining hozirgi `GEMINI_API_KEY`si Free tier'da bo'lgani sabab rasm generatsiya kvotasi ANIQ 0 — Google Cloud'da billing yoqilmaguncha `/img` xato qaytaradi (foydalanuvchiga tushuntirildi, hali qaror qilinmagan). Ikkalasi ham Sprint 3'ning rasmiy DoD/vazifa ro'yxatiga kirmaydi (Telegram bot funksionalligini kengaytirish jihatidan boshqa qo'shimchalarga o'xshash), alohida sprint fayli yo'qligi sababli shu yerga qayd etildi; CLAUDE.md'ga tegishli qarorlar allaqachon yozilgan. **Shu kunning davomida bir necha qo'shimcha topilma va tuzatish bo'ldi:** (1) commit `e2b876b` — jonli sinovda `generateImage()` aslida `image/png` emas, `image/jpeg` qaytarishi aniqlandi; funksiya endi `Promise<Buffer>` o'rniga `Promise<{buffer, mimeType}>` qaytaradi va Telegram'ga yuborilayotgan fayl kengaytmasi shu haqiqiy `mimeType`ga qarab tanlanadi. (2) commit `bca42b4` — jonli sinovda bir xil `/img` so'rovi birinchi safar Gemini'dan 503 ("high demand"/UNAVAILABLE) qaytarib, darhol keyingi urinishda muvaffaqiyatli bo'lgani kuzatildi; shu sabab `generateImage()`ga FAQAT 503 xatosida (boshqalarida, masalan 429 kvota, emas) bitta marta 3 soniya kutib qayta urinish qo'shildi. (3) Loyihaga aloqasi yo'q, sof infratuzilma hodisasi: foydalanuvchining local kompyuterida C disk butunlay to'lib qolgani (0 bayt bo'sh joy) Claude Code'ning fayl yozish/terminal operatsiyalarini buzgan; yechim sifatida butun loyiha `D:/loyihalar/yordamchi`ga ko'chirildi (GitHub'dan qayta clone + `.env.local`/`.vercel`/`.claude` qo'lda ko'chirish + `npm install`) — bundan buyon barcha ish shu yo'ldan davom etadi. (4) **ENG MUHIM TOPILMA** (commit `3234e54` va `8ce6e5a`): production'da Telegram webhook `/img` kabi sekin amallarda doimiy "Request timed out after 10000 ms" → 500 xato qaytarardi. Avval bu Vercel Hobby tarif chegarasi deb taxmin qilinib, `vercel.json`ga `{"fluid": true}` (Fluid Compute) qo'shildi — bu YORDAM BERMADI. Haqiqiy sabab: grammy kutubxonasining o'zi `webhookCallback()` ichida standart 10 soniyalik ICHKI timeout qo'yar ekan (Vercel'ga umuman aloqasi yo'q, `maxDuration` yolg'iz o'zi yetarli emas) — bu 2026-07-22'da (video-yuklab-berish qo'shilganda) ham aniqlanmagan/tuzatilmagan xato edi. Tuzatish: `webhookCallback(bot, "std/http", { timeoutMilliseconds: Infinity })` — grammy'ning ichki timeout o'ramini butunlay o'chirib, yagona chegara sifatida `maxDuration=60`ni qoldiradi. Jonli production sinovda tasdiqlandi (21.5s davom etgan so'rov muvaffaqiyatli 200 qaytardi, avval 10s'da 500 bilan tugardi). Barchasi CLAUDE.md'da batafsil qayd etilgan.
- **2026-07-23** — Sprint rejasidan yana tashqarida (commit `e30425a`, hujjatlashtirish `b1a5ed4`), asosiy Telegram bot (@helperizim_bot)ga **`/search <so'rov>`** buyrug'i qo'shildi — Gemini'ning `google_search` grounding tool'i orqali haqiqiy internet qidiruviga asoslangan javob beradi va mavzuga oid video havolasi qo'shadi (`searchWeb()`, `src/lib/gemini.ts`). Asosiy botda `bot.command("search", ...)` (shaxsiy chat) orqali, Telegram Business xabarlarida esa `/img`dagi bilan bir xil andozada qo'lda regex parse qilinadi (grammy'ning `bot.command()` filtri `business_message`ni tekshirmasligi sababli, avvalgi `/img` ishida aniqlangan cheklovga mos). Jonli sinovda ikkita cheklov aniqlandi: (1) Gemini grounding qaytargan manba havolalari doim Google'ning redirect linki, haqiqiy domen faqat "title" maydonida ko'rinadi; (2) hatto video ochiq so'ralganda ham grounding deyarli hech qachon haqiqiy YouTube natijasini qaytarmaydi, shu sabab "video" natija aksariyat holatda oddiy YouTube QIDIRUV havolasi (video fayl emas). Production'da jonli webhook orqali tasdiqlandi (200 status, ~8 soniya). Sprint 3'ning rasmiy DoD/vazifa ro'yxatiga kirmaydi (Telegram bot funksionalligini kengaytirish jihatidan boshqa qo'shimchalarga o'xshash), alohida sprint fayli yo'qligi sababli shu yerga qayd etildi; CLAUDE.md'ga tegishli qaror allaqachon yozilgan.
- **2026-07-23** — Sprint rejasidan yana tashqarida (commit `d02ebd2`, hujjatlashtirish `81e85ed`), asosiy Telegram bot (@helperizim_bot)ga **"/pdf" oqimi** qo'shildi (buyruq emas, oddiy PDF fayl yuborish orqali boshlanadi): foydalanuvchi PDF fayl yuborsa, bot avval "Nima qilay?" deb so'raydi (yangi `pdf_sessions` jadvali — chatId bo'yicha bitta kutilayotgan sessiya), keyingi matn yoki ovozli xabar ko'rsatma sifatida qabul qilinadi, Gemini PDF hujjatni to'g'ridan-to'g'ri (ko'p modalli hujjat tushunish, `application/pdf` inlineData) o'qib ko'rsatmani bajaradi, natija esa yangi PDF hujjatga aylantirilib (`pdf-lib`, sof JS/TS) qaytariladi. Jonli production sinovda haqiqiy Telegram `file_id` bilan to'liq zanjir (PDF yuborish → "nima qilay" so'rovi → ko'rsatma → yangi PDF) tasdiqlandi — ikkala webhook so'rovi ham 200 qaytardi va Gemini xulosasi haqiqiy PDF mazmuniga mos kelgani tekshirildi. Ma'lum cheklov: standart shrift faqat lotin skriptini qo'llab-quvvatlaydi (kirill/arab kabi skriptlar "?" bilan almashadi); hozircha faqat asosiy botning shaxsiy chatida ishlaydi (guruh/Business hali yo'q). Bu funksiya ham (avvalgi `/img`, `/search`, ovozli javob kabi) Sprint 1 dizaynida rejalashtirilmagan ad-hoc qo'shimcha, Sprint 3'ning rasmiy DoD/vazifa ro'yxatiga kirmaydi, alohida sprint fayli yo'qligi sababli shu yerga qayd etildi; CLAUDE.md'ga tegishli qaror allaqachon yozilgan. Checkbox o'zgartirish talab qilinmadi. **Tuzatish (2026-07-23, commit `4ee47ad`/`4fd8c7d`):** foydalanuvchi shikoyat bildirdi — AI PDF'ni tahrirlaganda butun hujjat dizaynini (asl shrift/joylashuv o'rniga oddiy qayta yaratilgan matn) buzib qo'yayotgan edi. Tuzatildi: Gemini endi ko'rsatma qaysi ASL sahifa(lar)ga tegishli ekanini aniqlaydi (`planPdfEdit()`, structured JSON: `pageNumbers`, `newText`, `summary`) va FAQAT o'sha sahifalar `applyPdfEdit()` orqali almashtiriladi — qolgan barcha sahifalar original PDF'dan pdf-lib `copyPages` bilan bayt darajasida aynan ko'chiriladi, dizayni 100% saqlanadi. Jonli 2 sahifali test PDF (Mars/Yupiter) va production'da haqiqiy Telegram `file_id` orqali tasdiqlandi. Qolgan cheklov: almashtirilgan sahifaning o'zi hamon oddiy shrift bilan qayta chiziladi.
- **2026-07-23** — Sprint rejasidan yana tashqarida (commit `64b1120`), mavjud **AI Assistant** (2026-07-21'da shu bo'limga qo'shilgan) kengaytirildi: mehmon/mijoz (egasi bo'lmagan birov) AI Assistant'ga egasiga mo'ljallangan aniq topshiriq/eslatma yozsa ("Yusufga ayt, non olib kelsin" kabi), tizim buni endi avtomatik aniqlaydi va (1) egasining "Vazifalar" ro'yxatiga qo'shadi, (2) egasiga Telegram orqali darhol xabar yuboradi. Yangi `answerGuestQuestion()` (`src/lib/assistant.ts`, mavjud `answerAssistantQuestion()`ning kontekst qatlamini qayta ishlatib bitta Gemini chaqiruvida ham javob, ham "ownerTask"ni aniqlaydi) va `notifyOwnerOfTask()` (`src/lib/owner-task.ts`) besh guest-facing kanalga ulandi: asosiy bot (begonalar), Telegram Business, ko'p-bot dinamik webhook, Mini App mehmon chat (matn va ovoz). Guruh AI Assistant ataylab qamrovdan tashqari qoldirildi. Build/lint/tsc va jonli DB sinovidan o'tdi. To'liq tafsilot CLAUDE.md'da (2026-07-23 yozuvi) allaqachon mavjud. Sprint 3'ning rasmiy DoD/vazifa ro'yxatiga kirmaydi, checkbox o'zgartirish talab qilinmadi.
