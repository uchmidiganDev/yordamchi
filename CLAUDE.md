# CLAUDE.md — Loyiha qarorlari jurnali

> Bu fayl loyiha davomida qabul qilingan texnik/mahsulot qarorlarini va
> avtomatlashtirish qoidalarini qayd etib boradi. Har bir yangi qaror shu
> yerga yoziladi — takrorlanmasin va kelajakdagi agentlar/ishtirokchilar
> kontekstni tez tiklay olsin.

## Qabul qilingan qarorlar

- **2026-07-14** — Auth.js (NextAuth) + Telegram Login Widget o'rniga Telegram bot deeplink (`/start` orqali) + custom JWT sessiya (`jose` kutubxonasi, httpOnly cookie) yondashuvi qo'llanildi. Sabab: foydalanuvchi faqat o'zining Telegram ID'si (8172404961) orqali kirishni talab qildi; bu tekshiruvni bot `/start` handlerida to'g'ridan-to'g'ri amalga oshirish Auth.js provayder sxemasidan ko'ra soddaroq va talabga aniqroq mos keladi.
- **2026-07-14** — Sprint 1 (Dizayn va Ekranlar) alohida joyda tayyorlanib, keyinroq loyihaga qo'shiladi deb kelishildi. Shu sababli DB/backend/avtorizatsiya ishi loyihada birinchi bajarilgan ish bo'lsa-da, hujjatlashtirilgan tartibda "Sprint 2" (`sprints/2-sprint-db-bekend-avtorizatsiya-crud.md`) rejasi doirasida bajarildi deb hisoblanadi.
- **2026-07-16** — Sana/vaqt formatlashda `toLocaleDateString`/`toLocaleString` bilan `"uz-UZ"` locale'ni bevosita ishlatish taqiqlanadi. Sabab: server (Node, to'liq ICU) va brauzer (qisman ICU) natijalari farq qilib, React hidratsiya xatosiga sabab bo'lgani preview'da sinab ko'rilganda aniqlandi. Yechim sifatida `src/lib/format-date.ts` da qo'lda o'zbekcha oy nomlari bilan formatlaydigan yordamchi funksiyalar (`formatDateUz`, `formatDateShortUz`, `formatTimeUz`) yaratildi — kelajakda ham shu faylni yoki xuddi shunday qo'lda formatlashni qo'llash kerak.
- **2026-07-16** — Desktop maketidagi "maqsad ichida subtask checklist" funksiyasi ataylab amalga oshirilmadi; o'rniga `tasks.goalId` orqali maqsad↔vazifa bog'lanishi ishlatildi. Sabab: bu yondashuv soddaroq va mobil maketdagi tuzilma bilan mos keladi.
- **2026-07-16** — Sprint 2 CRUD ishlaridan so'ng foydalanuvchi "yana kerakli bo'lim qo'shish"ni so'radi va tanlovni proaktiv ravishda qoldirdi; shu asosda "Sozlamalar" (`/sozlamalar`) sahifasi qo'shishga qaror qilindi. Sabab: bu ekran Sprint 1 dizaynida allaqachon "E9 Sozlamalar" sifatida rejalashtirilgan edi va `users` jadvalida ilgari yaratilgan, lekin hali ishlatilmayotgan `timezone`, `morningTime`, `eveningTime`, `googleRefreshToken` maydonlariga to'g'ridan-to'g'ri mos keladi — shu sababli boshqa yangi bo'lim o'rniga eng kam qo'shimcha ish bilan mavjud sxemani ishga tushirish uchun eng mantiqiy tanlov edi.
- **2026-07-16** — Google Calendar OAuth uchun `googleapis` npm paketi o'rniga to'g'ridan-to'g'ri `fetch` bilan Google OAuth2 token endpoint'lari chaqiriladi (`src/lib/google-oauth.ts`). Sabab: `googleapis` paketi hajmi katta va Vercel Edge/Serverless muhitida keraksiz; OAuth2 token almashtirish oddiy HTTP POST so'rovlari orqali amalga oshiriladi va bu paket bog'liqligini kamaytiradi.
- **2026-07-16** — Telegram botga Paynet o'tkazma cheki oddiy matn sifatida yuborilganda avtomatik parse qilinib xarajat sifatida saqlanadi (`src/lib/paynet-receipt.ts`, `src/lib/expense-from-receipt.ts`). Dedup uchun `expenses.external_ref` ustuni (tranzaksiya ID) qo'shildi. Sabab: foydalanuvchi Paynet chekini qo'lda kiritishdan qutilish uchun botni "oraliq kiritish qatlami" sifatida ishlatish — bu eng kam ishqalanish bilan xarajat yozishga imkon beradi.
- **2026-07-16** — Takrorlanuvchi vazifalar uchun occurrence-based model qo'llanildi: bir martalik vazifa holati `tasks.status` (pending/done) orqali, takrorlanuvchi vazifa esa `task_occurrences` jadvaliga kunlik yozuv (upsert/delete, `toggleOccurrence()`) orqali boshqariladi. Sabab: bu ikki xil mantiqni aralashtirib yubormaslik va takrorlanuvchi vazifa tarixini alohida jadvalda saqlash imkonini beradi — har kuni `tasks.status` ni qayta yozishdan ko'ra aniqroq va kengaytirish oson.
- **2026-07-16** — Loading holati uchun umumiy `ListSkeleton` yondashuvi qo'llanildi: bitta `src/app/(app)/list-skeleton.tsx` komponenti (shimmer animatsiya, `prefers-reduced-motion` qo'llab-quvvatlanadi) va har bir sahifa uchun Next.js `loading.tsx` fayllar. Sabab: har bir sahifada alohida skeleton komponenti yozishdan ko'ra bitta umumiy komponent va standart Next.js streaming yondashuvi kamroq kod va barqarorroq UX ta'minlaydi.
- **2026-07-16** — Telegram botga PDF fayl sifatida yuborilgan Paynet chekini qo'llab-quvvatlash uchun `unpdf` kutubxonasi ishlatiladi (`src/lib/pdf-text.ts` — `extractPdfText` funksiyasi); matn va PDF chek ishlash mantiqi `handleReceiptText` yordamchisiga ajratildi (`src/lib/telegram-bot.ts`); `next.config.ts`ga `serverExternalPackages: ["unpdf"]` qo'shildi. Sabab: foydalanuvchi Paynet chekini PDF fayl ko'rinishida ham yuborishi kerak edi; `unpdf` serverless muhitda `pdf.js`ga asoslangan, `canvas` bog'liqligi yo'q va Vercel'da ishonchli ishlaydi.
- **2026-07-16** — Telegram bot istalgan PDF'ni (Paynet formatida bo'lmasa ham) xarajat sifatida saqlash uchun umumiy fallback yondashuvi qo'llanildi (`src/lib/generic-receipt.ts` — `parseGenericReceipt`): summa aniqlashda avval valyuta kalit so'zi (so'm/sum/uzs) bilan kelgan raqamlar qidiriladi, topilmasa mingliklar ajratgichli (1 000 000 kabi) raqamlar olinadi; bir nechta kandidatdan eng kattasi jami sifatida tanlanadi; takroriy saqlashni oldini olish uchun matn sha256 hash'i `ref` sifatida ishlatiladi; xarajat "PDF cheklar" kartasiga "Chek" kategoriyasi bilan saqlanadi. Sabab: foydalanuvchi Paynet'dan tashqari har qanday PDF chekni bot orqali yuborib, avtomatik xarajat qo'shishni xohladi; bu yondashuv Paynet parser'ni asosiy, umumiy summa-ajratishni fallback sifatida ushlab turadi va PRD tashqarisidagi so'rovni minimal qo'shimcha kod bilan qo'llab-quvvatlaydi.
- **2026-07-20** — Gemini AI chaqiruvlari uchun `@google/generative-ai` npm paketi o'rniga to'g'ridan-to'g'ri `fetch` bilan Generative Language API (`generateContent`, `responseMimeType: application/json` + `responseSchema`) chaqiriladi (`src/lib/gemini.ts`). Sabab: `google-oauth.ts`dagi (2026-07-16) qaror bilan izchillik — paket bog'liqligini kamaytirish va Vercel serverless muhitida yengil ishlash; Google Calendar bilan ishlash uchun ham xuddi shu sababdan `googleapis` o'rniga fetch qo'llanildi (`src/lib/google-calendar.ts`) — bu 2026-07-16dagi googleapis-siz OAuth qaroridan Calendar API chaqiruvlariga kengaytirildi.
- **2026-07-20** — AI'ning A–E funksiyalari (kunlik xulosa, ertangi reja, ogohlantirish, progress, motivatsiya) alohida-alohida prompt/chaqiruv o'rniga BITTA strukturaviy Gemini chaqiruvida (`runManualAnalysis()`, `src/lib/actions/ai.ts`) amalga oshiriladi va yagona JSON natija `analyses` jadvaliga (`kind: manual`) saqlanadi; F funksiyasi (maqsadni kichik vazifalarga bo'lish, `suggestGoalTasks()`) alohida chaqiruv sifatida qoldi. Sabab: A–E bir xil kontekst (bugungi holat, band vaqtlar, maqsadlar)dan foydalanadi va bitta javobda mantiqiy bog'liq — alohida-alohida chaqirish ortiqcha API xarajati va murakkablik keltiradi; F esa boshqa kirish (bitta maqsad) va boshqa chiqish shakli (vazifalar ro'yxati) talab qiladi, shu sababli alohida qoldirildi.
- **2026-07-20** — Vaqti belgilangan BIR MARTALIK vazifalar Google Calendar'ga avtomatik sinxronlanadi (`tasks.google_event_id`, `src/lib/actions/tasks.ts`), lekin takrorlanuvchi vazifalar ataylab sinxronlanmaydi. Sinxron "best-effort" — Calendar API xatosi vazifa CRUD amalini to'xtatmaydi. Sabab: takrorlanuvchi vazifani Google Calendar'da to'g'ri ifodalash uchun RRULE va occurrence-darajasidagi moslashtirish kerak bo'lardi, bu murakkablikni oshiradi; bir martalik vazifa esa oddiy event sifatida to'g'ridan-to'g'ri mos keladi. Kerak bo'lsa, takrorlanuvchi vazifa sinxroni keyingi sprintda alohida ko'rib chiqiladi.
- **2026-07-20** — Sprint 3 uchun jonli smoke-test skripti qo'shildi: `scripts/smoke-sprint3.ts` (`npx tsx scripts/smoke-sprint3.ts`) — haqiqiy `GEMINI_API_KEY` va DB ma'lumotlari bilan Gemini ulanishini, kontekst qatlamini (`buildAiContext`) va A–E tahlilni tekshiradi; Google ulangan bo'lsa kalendarga vaqtinchalik event yaratib darhol o'chiradi. Sabab: `GEMINI_API_KEY` kiritilgach kod yozmasdan (UI orqali qo'lda bosmasdan) tezkor va takrorlanuvchan jonli tekshiruv imkonini berish uchun; kelajakda Gemini/Calendar bilan bog'liq o'zgarishlarni tekshirishda shu skriptdan foydalanish tavsiya etiladi.
- **2026-07-21** — Google Cloud loyihasi ("My Project 10779", `robotic-totem-472718-i0`) OAuth consent screen hozircha "Testing" rejimida — bu rejimda faqat Google Cloud Console > APIs & Services > OAuth consent screen > Audience > Test users bo'limiga qo'lda qo'shilgan Google hisoblar ulanishi mumkin, aks holda "Error 403: access_denied — has not completed the Google verification process" chiqadi. `tommysamiyyusuf@gmail.com` shu ro'yxatga test user sifatida qo'shilgan va shu orqali Google Calendar muvaffaqiyatli ulandi. Sabab: kelajakda boshqa Google hisob bilan ulashga urinilganda (avval shu ro'yxatga qo'shish kerakligini eslatish uchun) yoki ilova production'ga (Google verification'dan o'tgan) chiqarilishi kerak bo'lganda bu kontekst zarur bo'ladi.
- **2026-07-21** — PRD/Sprint 1 ekran ro'yxatida bo'lmagan yangi "AI Assistant" funksiyasi (Bilim bazasi + moslashtiriladigan System Prompt + Telegram orqali erkin savol-javob) qo'shildi: yangi `knowledge_entries` jadvali va `/bilim-baza` admin sahifasi (yozuvlar CRUD + System Prompt kartasi), `src/lib/assistant.ts`dagi `answerAssistantQuestion(userId, question)` foydalanuvchi System Prompt'i + barcha bilim bazasi yozuvlarini bitta Gemini so'roviga birlashtiradi. Sabab: foydalanuvchi Telegram botni to'liq shaxsiy AI Assistant sifatida (o'z bilim bazasidan foydalangan holda) ishlatishni so'radi; bu mavjud A–F tahlil funksiyalaridan (sprints/3) alohida, umumiy suhbat rejimi.
- **2026-07-21** — `answerAssistantQuestion(userId, question)` `userId`ni to'g'ridan-to'g'ri parametr sifatida qabul qiladi, sessiya/cookie orqali emas. Sabab: funksiya Telegram bot xabar handleridan ham chaqiriladi (`src/lib/telegram-bot.ts`), u yerda Next.js so'rov konteksti (`cookies()`) mavjud emas — parametr sifatida uzatish funksiyani ham veb (kelajakda), ham bot kontekstida qayta ishlatish imkonini beradi.
- **2026-07-21** — Standart AI Assistant system prompt matni (`DEFAULT_ASSISTANT_SYSTEM_PROMPT`) `"use server"` direktivali server-action fayldan ajratilib, alohida oddiy `src/lib/assistant-prompt.ts` fayliga chiqarildi. Sabab: Next.js'da `"use server"` fayllar FAQAT async funksiya export qila oladi — oddiy konstanta shu fayl bilan birga eksport qilinsa, client komponentda import qilinganda React "Invalid value for prop" xatosi bilan buziladi (jonli sinovda aniqlanib tuzatildi).
- **2026-07-21** — Telegram botning matn xabar handleri (`bot.on("message:text")`) qayta qurildi: ilgari chek bo'lmagan har qanday matn jim tashlab qo'yilardi (`silentIfNotReceipt: true`); endi `handleReceiptText()` chek emasligini (`false`) qaytarsa, xabar `handleAssistantMessage()` orqali AI Assistant'ga (`answerAssistantQuestion`) yuboriladi va javob foydalanuvchiga qaytariladi. Sabab: bot endi nafaqat chek qabul qiluvchi, balki to'liq AI Assistant sifatida ishlashi kerak — Paynet chek tekshiruvi birinchi ustuvorlik, qolgan barcha matn AI Assistant fallback'iga o'tadi.
- **2026-07-21** — `/kalendar` sahifasiga tezkor vazifa qo'shish ("+" FAB, `kalendar-add-button.tsx`) qo'shildi va vazifa CRUD amallariga (`createTask`, `updateTask`, `toggleTaskStatus`, `toggleOccurrence`, `deleteTask`) `revalidatePath("/kalendar")` qo'shildi. Sabab: foydalanuvchi kalendar ko'rinishidan to'g'ridan-to'g'ri vazifa qo'sha olishini so'radi; mavjud `createTask()` action qayta ishlatildi (yangi backend kod yozilmadi), shu bilan birga vazifa→Google Calendar avtomatik sinxron mexanizmidan ham foydalanildi va kalendar ko'rinishi vazifa o'zgarishidan keyin darhol yangilanadigan bo'ldi.
- **2026-07-21** — Yagona qattiq yozilgan "ommaviy bot" (`users.publicBotEnabled` ustuni, `public-bot.ts`, `/api/telegram/public-webhook`) olib tashlanib, o'rniga DB'ga asoslangan dinamik ko'p-bot tizimi qurildi: yangi `telegram_bots` (id, userId, token, username, enabled) va `telegram_messages` (id, botId, userId, chatId, fromName, fromUsername, text, answer, createdAt) jadvallari (migratsiya `drizzle/0007_mushy_sheva_callister.sql` — avval `public_bot_enabled` ustuni qo'shildi, so'ng `drizzle/0008_steep_johnny_storm.sql` — o'sha ustun olib tashlanib ikki yangi jadval yaratildi); yangi `/telegram` sahifasi (botlar ro'yxati — Start/Stop, o'chirish; "Bot qo'shish" formasi — token Telegram `getMe` orqali tekshiriladi → DB'ga saqlanadi → webhook avtomatik ro'yxatdan o'tkaziladi; xabarlar jurnali); yangi `src/lib/telegram-api.ts` — grammy'siz, to'g'ridan-to'g'ri fetch orqali Telegram Bot API (getMe, sendMessage, sendChatAction, setWebhook, deleteWebhook); yangi umumiy dinamik webhook route `src/app/api/telegram/bots/[botId]/webhook/route.ts` — istalgan sonli bot uchun bitta handler; `src/lib/assistant.ts`dagi `answerAssistantQuestion()` endi ixtiyoriy `history: ConversationTurn[]` parametrini qabul qiladi — suhbat konteksti (oldingi savol-javoblar) `telegram_messages`dan o'qilib keyingi Gemini chaqiruviga uzatiladi; `src/lib/public-reply.ts` endi "kamida bitta `telegram_bots` yozuvi yoqilganmi" tekshiradi (avvalgi yagona `publicBotEnabled` ustuni o'rniga). Sabab: foydalanuvchi o'zining shaxsiy helperizim_bot'idan tashqari istalgan sondagi qo'shimcha Telegram botni ulab, ularning barchasi bir xil bilim bazasi/System Prompt asosidagi AI Assistant orqali javob berishini va har suhbatning konteksti (tarixi) saqlanishini so'radi; DB'ga asoslangan yondashuv har yangi bot uchun kod o'zgartirish/qayta deploy qilishni talab qilmaydi, bitta umumiy webhook handler barcha botlarga xizmat qiladi.
- **2026-07-21** — Yangi environment o'zgaruvchi nomlarida `PUBLIC_` prefiksidan (masalan `PUBLIC_BOT_TOKEN`) qochish kerak. Sabab: ko'p-bot tizimi uchun Vercel'ga shu prefiks bilan maxfiy token qo'shishga urinilganda Vercel CLI ogohlantirdi — `PUBLIC_` prefiksi boshqa freymvorklarning (masalan Vite/CRA) client-side env konventsiyasida brauzerga OCHIQ deb hisoblanadi. Muammo darhol aniqlanib, noto'g'ri nomlangan o'zgaruvchi Vercel'dan o'chirildi va xavfsiz nomga (`TG_PUBLIC_BOT_WEBHOOK_SECRET`) o'zgartirildi; butun loyiha tekshirilib, maxfiy qiymat saqlaydigan hech qanday `PUBLIC_` bilan boshlanuvchi nom qolmagani tasdiqlandi (Next.js'ning o'zining `NEXT_PUBLIC_APP_URL` konventsiyasi va `src/proxy.ts`dagi ichki `PUBLIC_PATHS` massivi bundan mustasno — ular maxfiy qiymat emas, shuning uchun xavfsiz). Kelajakda har qanday yangi env o'zgaruvchisi nomlashda shu qoidaga rioya qilish kerak.
- **2026-07-21** — PRODUCTION INTSIDENT: foydalanuvchi yangi `/telegram > Bot qo'shish` formasiga tasodifan o'zining shaxsiy helperizim_bot tokenini yubordi — bu botning webhook'ini yangi umumiy `/api/telegram/bots/[botId]/webhook` route'iga o'zgartirib yubordi va natijada Telegram asosiy `/api/telegram/webhook`ga "307 Temporary Redirect" xatosi bilan xabar yetkaza olmay qoldi, chek/login funksiyalari vaqtincha buzildi. Darhol tuzatildi: (1) noto'g'ri qo'shilgan yozuv `telegram_bots` jadvalidan o'chirildi, (2) helperizim_bot webhook'i to'g'ri manzilga (`/api/telegram/webhook`) qaytarildi va pending update'lar tozalandi, (3) `addTelegramBot()` action'iga himoya qo'shildi (`src/lib/actions/telegram-bots.ts`) — endi `getMe` orqali olingan `username` `TELEGRAM_BOT_USERNAME` (helperizim_bot) bilan mos kelsa, aniq xatolik xabari bilan rad etiladi. Sabab: kelajakda xuddi shu xatoning (shaxsiy bot tokenini boshqa formaga yuborib, uning webhook'ini tasodifan o'zgartirib qo'yish) qayta yuz berishini ilova darajasida oldini olish.
- **2026-07-21** — Telegram Business orqali shaxsiy akkauntga AI Assistant avtomatik javob qo'shildi (commit `b36636d`). MUHIM FARQ: Telegram Business — bu MTProto/userbot (shaxsiy akkauntni tashqi kutubxona bilan avtomatlashtirish, Telegram ToS'ga zid, akkaunt bloklanish xavfi bor) EMAS, balki Telegram Bot API'ning RASMIY qismi ("Business Connection" funksiyasi, 2024-yilda joriy qilingan) — foydalanuvchi Telegram > Sozlamalar > Telegram Business > Chatbots orqali botni o'zi ulaydi, hech qanday ToS buzilishi yo'q. Amalga oshirish: `users.business_connection_id`/`business_connection_enabled` ustunlari (`drizzle/0009_complete_hawkeye.sql`) `bot.on("business_connection")` orqali yoziladi; yangi `business_messages` jadvali (`telegram_messages`ga o'xshash, lekin `telegram_bots`ga bog'liq emas — helperizim_bot bu jadvalga kirmasligi haqidagi 2026-07-21dagi qarorga mos) mijoz-suhbat tarixini saqlaydi; `bot.on("business_message")` javob beradi. Telegram Business yangilanishi suhbatning HAR IKKI tomonini (mijoz xabarlari HAM, egasining o'z telefonidan yozgan javoblari HAM) o'z ichiga oladi, shu sababli `msg.from.id === ALLOWED_TELEGRAM_ID` bo'lgan xabarlar SHART ravishda e'tiborsiz qoldiriladi — aks holda AI egasining o'z javobiga ham "javob" berishga urinadi. `/telegram` sahifasiga "Telegram Business" holat kartasi va "Business xabarlar" jurnali qo'shildi (`getBusinessConnectionStatus()`, `listBusinessMessages()`).
- **2026-07-21** — `answerAssistantQuestion()`ga ixtiyoriy `senderName?: string | null` parametri qo'shildi (commit `439582a`) — berilsa, promptga "SUHBATDOSH: Ismi: {name}..." qatori qo'shilib, AI javobida suhbatdoshning ismidan tabiiy ravishda foydalanadi (majburiy takrorlamasdan). Barcha begonalarga javob beruvchi yo'llarga ulandi: Telegram Business, ko'p-bot dinamik webhook, helperizim_bot ommaviy javob; ism Telegramdan (`first_name`+`last_name`, bo'lmasa `username`) olinadi. Sabab: foydalanuvchi shaxsiy kotib uslubiga mos ravishda AI javoblarida suhbatdoshning ismi ishlatilishini so'radi. Eslatma: shu sessiyada Gemini API kvota chegarasiga (429 xatosi) juda ko'p test chaqiruvidan so'ng duch kelindi — kelajakda jonli sinovlarni rejalashtirishda buni hisobga olish kerak.
- **2026-07-21** — SABOQ: `setWebhook` chaqirilganda `allowed_updates` ko'rsatilmasa, Telegram faqat standart yangilanish to'plamini yuboradi — bu to'plam `business_connection`/`business_message` kabi Telegram Business yangilanishlarini O'Z ICHIGA OLMAYDI. Shu sabab foydalanuvchi botni Business orqali ulagandan keyin ham xabarlar avtomatik javob olmayotgan edi (webhook ilgari `allowed_updates`siz ro'yxatdan o'tkazilgan edi). `src/lib/telegram-api.ts`dagi `setWebhook()` endi ixtiyoriy `allowedUpdates: string[]` parametrini qabul qiladi (yangi `getWebhookInfo()` diagnostika uchun qo'shildi); helperizim_bot'ning production webhook'i to'liq ro'yxat (`["message", "business_connection", "business_message", "edited_business_message", "deleted_business_messages"]`) bilan bir martalik to'g'ridan-to'g'ri Telegram API chaqiruvi orqali qayta ro'yxatdan o'tkazildi. Sabab: kelajakda boshqa bot qo'shilganda yoki webhook qayta o'rnatilganda ham shu to'liq ro'yxat ishlatilishi kerak, aks holda Business xabarlari yana kelmay qo'yadi.
- **2026-07-22** — Yangi ko'p bosqichli **"Telefon AI"** loyihasi (haqiqiy PSTN telefon qo'ng'iroqlariga AI javob berish, Telegram emas) uchun arxitektura strategiyasi belgilandi: foydalanuvchining shaxsiy raqami (+998...) o'zgarmaydi; operatorning shartli yo'naltirish (conditional call forwarding, GSM standart kodlari — `**61*` javob bermasa, `**67*` band bo'lsa, `**62*` tarmoqdan tashqarida) xizmati orqali FAQAT javobsiz qolgan qo'ng'iroqlar alohida virtual raqamga yo'naltiriladi. Telefoniya provayderi sifatida dastlab Twilio ko'rib chiqilgan, lekin O'zbekiston raqami bilan tasdiqlashda "[0x07] Verification cannot be sent" xatosiga uchragani sabab **Telnyx** tanlandi (veb qidiruv orqali O'zbekiston raqamlarini taklif qilishi tasdiqlangan — telnyx.com/phone-numbers/uzbekistan); Plivo ham ko'rib chiqilgan, lekin O'zbekiston qo'llab-quvvatlashi tasdiqlanmagani sabab rad etilgan. Foydalanuvchiga tushuntirilgan MUHIM haqiqat: haqiqiy telefon raqami HECH QACHON butunlay bepul bo'lmaydi (raqam ijarasi + daqiqalik to'lov) — bu PSTN infratuzilma xarajati bo'lgani uchun istisnosiz barcha provayderlarda shunday; shu sabab "o'zimiz qilamiz" variantlari (soxta raqam, Telegram-raqam-sotuvchi botlar, GSM gateway+SIM) ko'rib chiqilib rad etilgan (texnik jihatdan mos kelmaydi, yo baribir pul talab qiladi, yo noqonuniy/amaliy emas). Foydalanuvchi hozircha Telnyx orqali kichik xarajat bilan davom etish yoki hozircha faqat Telegram (bepul) bilan cheklanish orasida qaror qabul qilmagan. Sabab: bu qarorlar loyihaning eng qimmat/murakkab kelgusi bosqichi (Stage 2: Telnyx ulanishi) uchun zamin — kelajakda davom ettirilganda ushbu tanlov tarixini qayta muhokama qilmaslik uchun yozib qo'yildi.
- **2026-07-22** — "Telefon" bo'limi Stage 1'i (hech qanday tashqi pullik xizmatga bog'liq bo'lmagan qism) amalga oshirildi (commit `29429cb`): yangi `ai_personas` jadvali (id, userId, name, systemPrompt, createdAt, updatedAt) — har biri o'z nomi va System Prompt'iga ega AI "shaxs"; `users.active_persona_id` (FK constraintsiz — ataylab, chunki nullable "joriy tanlangan" ko'rsatkich, single-tenant ilovada oddiy bo'lishi uchun) va `users.phone_ai_enabled` (boolean, default false — Start/Stop) ustunlari; `src/lib/actions/personas.ts` CRUD (`listPersonas()` birinchi chaqiruvda birorta ham persona yo'q bo'lsa standart "Yusuf AI"ni avtomatik yaratib faol qiladi); yangi `/telefon` sahifasi (Start/Stop switch — hozircha FUNKSIONAL EMAS, telefoniya hali ulanmagani sabab faqat holatni saqlaydi — + personalar ro'yxati: qo'shish/tahrirlash/o'chirish/faol qilish, Bilim baza sahifasidagi CRUD naqshiga mos), nav'ga "Telefon" tabi. Shuningdek qaror qilindiki, AI o'chirilgan (Stop) holatda qo'ng'iroqqa oddiy "qo'ng'iroqni tugatish" emas, balki "xabar qoldiring" tarzidagi ovozli xabar aytilib, audio sifatida yoziladi (hali amalga oshirilmagan). Kelgusi bosqichlar rejasi (hali boshlanmagan): Stage 2 — Telnyx ulanishi (foydalanuvchi o'zi hisob ochib API kalit+raqam berishi kerak, bu ilova kodidan tashqarida bajariladigan qadam); Stage 3 — ovozli suhbat oqimi (STT→AI→TTS, navbat bilan); Stage 4 — audio yozib saqlash; Stage 5 — suhbatlar ro'yxatini `/telefon` sahifasida ko'rsatish. Sabab: bu katta loyihani kichik, mustaqil tekshiriladigan bosqichlarga bo'lib borish — Stage 1 hech qanday tashqi xizmatga muhtoj emas va darhol UI'da sinab ko'rish mumkin.
- **2026-07-22** — "Telefon AI" Stage 2 (Telnyx ulanishi) uchun webhook skeleti tayyorlandi, lekin hali FAOL EMAS: `src/lib/telnyx.ts` (fetch-based Telnyx Call Control klienti — `answerCall`, `speakCallText`, `hangupCall`, Ed25519 webhook imzo tekshiruvi `verifyTelnyxSignature`, googleapis/gemini.ts'dagi kabi SDK'siz yondashuv) va `src/app/api/telnyx/webhook/route.ts` (call.initiated→answer, call.answered→persona nomi bilan test TTS xabari, call.speak.ended→hangup). Foydalanuvchi Telnyx hisobini ochishga urindi, lekin hisob "Pretrial" holatida qolib ketdi — bu rejimda faqat 1 dona AQSh raqami bepul beriladi, O'zbekiston kabi global raqamlar uchun akkauntni to'lov usuli qo'shib upgrade qilish kerak (portal buni aniq ko'rsatadi: "Pretrial accounts can buy 1 USA, local phone-number... For global number coverage, please upgrade"). Foydalanuvchi hali upgrade qilmagan — shu sabab TELNYX_API_KEY/TELNYX_PUBLIC_KEY/TELNYX_PHONE_NUMBER `.env.local`da bo'sh qoldirilgan va Stage 2 harakatsiz. Kelgusi qadam: foydalanuvchi hisobni upgrade qilib, raqam+kalitlarni bergach, webhook'ni sinash.
- **2026-07-22** — Asosiy Telegram bot (@helperizim_bot, `src/lib/telegram-bot.ts`)ga Ovozli xabar (Voice Message) qo'llab-quvvatlash qo'shildi: foydalanuvchi so'ragan texnologiyalar ro'yxati (python-telegram-bot, Whisper, ElevenLabs, PostgreSQL, services/handlers/ai/voice/utils/config papka tuzilishi) mavjud loyiha stekiga (Next.js/TypeScript, grammy, Drizzle+Postgres, Gemini fetch-based) mos kelmagani sabab, o'sha talablar SHU MAVJUD stekda amalga oshirildi — parallel Python xizmati qo'shish kod bazasini bo'lib yuborardi va bilim bazasi/System Prompt mantiqini takrorlagan bo'lardi. STT ham, TTS ham alohida provayder (Whisper/ElevenLabs) o'rniga bitta `GEMINI_API_KEY` orqali Gemini'ning ko'p modalli imkoniyatlari bilan amalga oshirildi (`src/lib/gemini.ts`ga qo'shilgan `transcribeAudio()` — audio inlineData + transkripsiya prompt; `synthesizeSpeech()` — `responseModalities: ["AUDIO"]` + `speechConfig.voiceConfig.prebuiltVoiceConfig`, natija 16-bit/24kHz PCM, tashqi ffmpeg/kutubxonasiz oddiy 44-baytli WAV sarlavhasi bilan o'raladi). Yangi `src/lib/voice-reply.ts` (`sendVoiceReply()`) ikkala javob yo'liga (`handleAssistantMessage`, `public-reply.ts`dagi `replyAsPublicAssistant`) ixtiyoriy `withVoice` parametri orqali ulandi — oddiy matn savollar hech narsa o'zgarmadi (voice yo'q), faqat ovozli xabardan kelgan savollarga matn+ovoz ikkalasi qaytariladi. MUHIM TOPILMA (jonli test orqali aniqlandi): (1) Gemini TTS rasmiy ravishda o'zbek tilini qo'llab-quvvatlamaydi (faqat rus/ingliz 90+ til ro'yxatida), lekin amaliyotda test qilinganda o'zbekcha matnni tushunarli darajada tabiiy talaffuz bilan aytib berdi — rasmiy hujjatlardagi cheklov amaliy natijaga to'liq mos kelmadi; (2) Gemini STT ba'zan xatosiz o'zbek/rus/ingliz audio uchun ham `finishReason: "PROHIBITED_CONTENT"` qaytarishi mumkin — WAV formatidagi audio har doim ishlagan, lekin ffmpeg orqali generic sozlamalar (`-c:a libopus`, 24kHz, application belgilanmagan) bilan kodlangan OGG/Opus fayl BARQAROR ravishda (4/4 marta) shu xatoni bergan, `application=voip, 48kHz` bilan kodlangan xuddi shu audio esa muvaffaqiyatli o'tgan — bu Telegram'ning haqiqiy ovozli xabar kodlash parametrlariga (nutq uchun optimallashtirilgan, voip-ga o'xshash) mos kelishi ehtimoli yuqori, lekin 100% kafolatlanmagan. Shu sabab kod ataylab "eng yaxshi urinish" tarzida qoldirildi: agar Gemini STT bo'sh natija qaytarsa (blok yoki boshqa sabab), foydalanuvchiga aynan so'ralgan "Kechirasiz, ovozni tushuna olmadim..." xabari ko'rsatiladi — bu ham to'g'ri fallback, ham chinakam tushunmaslikni, ham noto'g'ri classifier blokini bir xil, xavfsiz tarzda qamrab oladi. Kelajakda haqiqiy Telegram ovozli xabarlar bilan false-positive tez-tez chiqsa, OGG/Opus'ni Gemini'ga yuborishdan oldin PCM/WAV'ga dekodlash (pure-JS/WASM opus decoder, masalan) qo'shish kerak bo'ladi. Shuningdek: `message:photo` (Vision AI) handleri loyihada UMUMAN MAVJUD EMAS — foydalanuvchi so'ragan 7-talab ("mavjud Vision AI ishlashda davom etsin") shu sababli qo'llanilmadi, chunki saqlanadigan mavjud funksiya yo'q edi.
- **2026-07-22** — Asosiy Telegram botga AI Website Analyzer funksiyasi qo'shildi (`src/lib/website-analyzer.ts`, yangi `website_analyses` jadvali — id, userId (doim ilova egasi, businessMessages naqshiga mos), chatId, url, html, analysis, createdAt; migratsiya `drizzle/0011_shiny_random.sql`). Foydalanuvchi yana Python/services-handlers-utils papka tuzilishini so'radi, lekin oldingi (ovozli xabar) qarorga mos ravishda mavjud TypeScript/Gemini stekda amalga oshirildi. Ishlash tartibi: foydalanuvchi butun xabar sifatida bitta `http(s)://` havola yuborsa (`extractSoleUrl()` — havola boshqa matn bilan aralashgan bo'lsa ishlamaydi, ataylab), sayt HTML manba kodi va muhim response headerlar (CSP, HSTS, X-Frame-Options va h.k.) olinadi (`fetchSiteHtml()`, 30 000 belgigacha kesiladi, 10s timeout, faqat text/html) va Gemini'ga so'ralgan 18 mezon + aniq 8 bo'limli emoji format (⭐✅❌🚀🎨⚡🔒📱) bilan yuboriladi. MUHIM CHEKLOV: bu HAQIQIY BRAUZER RENDER/SKRINSHOT EMAS — Puppeteer kabi og'ir bog'liqlik (Vercel serverlessga mos kelmaydi) qo'shilmadi, shuning uchun UI/UX, rang kontrasti, shriftlar kabi vizual mezonlar Gemini tomonidan faqat HTML/CSS manba kodidagi belgilar (class nomlari, inline style, media query, viewport tegi) asosida taxmin qilinadi — bu promptda ochiq aytilgan. "To'g'rila" (yoki "to'g'irla"/"tuzat" kabi imlo variantlari, `isFixRequest()` apostrof farqlaridan qat'i nazar taniydi) deyilganda shu suhbatdagi ENG OXIRGI tahlil (`getLatestAnalysis()`, chatId bo'yicha) asosida yaxshilangan to'liq HTML fayl yaratilib (`improveWebsite()`), har doim `.html` hujjat sifatida yuboriladi (Telegram Markdown parse_mode orqali emas — HTML/CSS kodidagi `_`/`*` belgilari "can't parse entities" xatosiga sabab bo'lishi mumkin edi). Tahlil hisoboti 4096 belgi chegarasidan oshsa `replyLong()` orqali bir necha xabarga bo'linadi. Jonli sinovda (https://example.com) butun zanjir — fetch→tahlil→format→tuzatilgan HTML — muvaffaqiyatli ishlagani tasdiqlandi. `drizzle-kit migrate` CLI buyrug'i bu muhitda vaqtincha osilib qolgani sabab (websocket transport muammosi bo'lishi mumkin, ildizi aniqlanmadi — loyihaning drizzle migratsiya tracking jadvali ham allaqachon 12 tadan atigi 2 tasini qayd etgani sababli avvaldan nomukammal edi), migratsiya SQL fayli to'g'ridan-to'g'ri `@neondatabase/serverless` Pool orqali qo'lda bajarildi — kelajakda shunga o'xshash holatda ham shu yondashuv (`drizzle/XXXX_*.sql`ni o'qib, `--> statement-breakpoint` bo'yicha bo'lib, Pool.query() bilan ishga tushirish) ishonchli alternativa sifatida ishlatilishi mumkin.
- **2026-07-22** — Asosiy Telegram botga AI Coding Assistant funksiyasi qo'shildi (`src/lib/code-assistant.ts`, yangi `code_knowledge_entries` va `code_reviews` jadvallari + `users.code_assistant_system_prompt` ustuni; migratsiya `drizzle/0012_careless_penance.sql`, oldingi migratsiya kabi `drizzle-kit migrate` osilib qolgani sabab qo'lda Pool orqali bajarildi). Foydalanuvchi yana Python Async + services/handlers/utils papka tuzilishini so'radi, lekin bir xil sabab bilan (mavjud loyiha TypeScript/Gemini stekida) shu stekda amalga oshirildi. Ishlash tartibi: foydalanuvchi kod yuborsa (``` bilan o'ralgan yoki xom holda — `extractCode()` avval fence qidiradi, topilmasa STRONG/WEAK naqshlar orqali taxmin qiladi: `def foo(`, `class`, `#include`, `package main`, HTML teg kabilar yolg'iz o'zi yetarli, `const`/`;`/`{`$ kabi zaifroq belgilardan esa kamida 3 tasi kerak — bu tuzatish jonli testda aniqlandi: dastlabki versiya faqat 2 ta ZAIF belgi talab qilardi va shu sabab Python kabi jingalak qavs/nuqta-vergulsiz tillarni ANIQLAY OLMAGAN edi, chunki `def foo():` kabi bitta signal yetarli emas edi), Gemini orqali `generateJson()` (mavjud `gemini.ts`dagi funksiya) bilan strukturaviy JSON javob (`language`, `message`, `code`) olinadi va shu chatga saqlanadi (`code_reviews`, chatId bo'yicha). "Fix"/"Explain"/"Optimize"/"Test"/"Integration Test"/"README"/"Docs"/"API Docs" (aniq shu inglizcha so'zlar, katta-kichik harfga sezgir emas) buyruqlari shu suhbatdagi ENG OXIRGI kod ustida ishlaydi va natija (agar kod qaytarilgan bo'lsa yangi kod, aks holda avvalgi kod) yana saqlanadi — shu bilan Fix→Optimize→Explain kabi zanjir ketma-ket oldingi natija ustida davom etadi (har bir keyingi buyruq avvalgi buyruqning chiqishi ustida ishlaydi, asl kod ustida emas). Har javob HTML `parse_mode` bilan yuboriladi (Telegram Markdown emas — website-analyzer'dagi xuddi shu sababga ko'ra: ixtiyoriy kod ichidagi `_`/`*` belgilari MarkdownV2'da "can't parse entities" xatosiga sabab bo'lardi; HTML'da faqat `&`/`</>` uchtasini escape qilish kifoya, ancha ishonchli); uzun kod (3000 belgidan ortiq) `<pre>` blok o'rniga fayl sifatida yuboriladi. Yangi `/kod-yordamchi` sahifasi (`/bilim-baza`dan aynan bir xil CRUD+System-Prompt naqsh bilan) — foydalanuvchi "Knowledge Base bilan integratsiya, alohida bo'lim" deb so'ragani uchun UMUMIY `knowledge_entries`dan ATAYLAB ALOHIDA jadval (`code_knowledge_entries`) ishlatildi, umumiy AI Assistant bilim bazasi bilan aralashmasligi uchun. Jonli sinovda haqiqiy xatoli Python kod (bo'sh ro'yxatda ZeroDivisionError) bilan tekshirilganda tahlil to'g'ri xatoni topib, aniq 3 qatorli ⭐ Code Quality/Performance/Security baholash bilan tugagani tasdiqlandi. ESLATMA: shu kunda (2026-07-22) juda ko'p jonli Gemini test chaqiruvidan so'ng 429 (kvota) xatosiga yana duch kelindi (ilgari 2026-07-21'da ham xuddi shunday holat qayd etilgan) — kelajakda bir kunda ko'p funksiya ustida ketma-ket jonli sinov o'tkazishda buni hisobga olish kerak, ayniqsa yangi funksiya qo'shilganda oxirgi tekshiruvlarni ehtiyotkorlik bilan tanlash lozim (hammasini emas, faqat eng muhim/yangi qismini sinash).
- **2026-07-22** — Asosiy Telegram botga AI Group Moderation (Anti-Spam) tizimi qo'shildi, lekin ATAYLAB Stage 1 ko'lamida (`src/lib/group-moderation.ts`, `src/lib/group-assistant.ts`; yangi jadvallar `group_settings`, `group_warnings`, `group_recent_messages`, `group_moderation_log`, `group_messages`; migratsiya `drizzle/0013_luxuriant_arachne.sql`, qo'lda Pool orqali bajarildi). Foydalanuvchi so'ragan to'liq tizim (Anti-Spam + AI Reklama filtri + AI So'kinish filtri + AI Moderation + Guruhda AI javob + AI Memory + barcha admin buyruqlari `/filter`, `/mute`, `/ban`, `/purge`, `/settings`, `/logs` + Statistika admin panel) haqiqatda JUDA katta ko'lam edi — Telefon AI'dagi bosqichlash yondashuviga o'xshab, ikkiga bo'lindi: Stage 1 (hozir amalga oshirildi) — to'liq aniqlash+ijro zanjiri (flood/duplicate/caps/emoji/username-bot-forward reklamasi deterministik, so'ng BITTA birlashtirilgan Gemini chaqiruvida reklama+so'kinish+risk darajasi — A-E funksiyalarini bitta chaqiruvga birlashtirish qaroriga (2026-07-20) mos), delete/warn/mute/ban ijrosi, guruh AI assistanti (@mention/reply/`/ai` `/ask` `/chat`, mavjud umumiy Bilim baza+System Prompt qayta ishlatilgan, guruh bo'yicha alohida xotira `group_messages`), va zarur admin buyruqlari (`/antispam on|off`, `/mute` `/unmute` `/ban` `/unban` `/kick` `/warn` — barchasi `getChatMember` orqali faqat guruh administratoriga ruxsat beradi). Stage 2 (keyinroq) — maxsus kalit so'z filtrlari (`/filter add|remove`), `/purge`, `/logs`, va veb statistika sahifasi — bu qism kechiktirildi, chunki kerakli ma'lumot (moderatsiya jurnali) Stage 1'dayoq `group_moderation_log`ga yozib borilyapti, shuning uchun Stage 2 shunchaki shu ma'lumot ustidan o'qish interfeysi bo'ladi. Yana Python/Redis/PostgreSQL/handlers-services-moderation-ai-database papka tuzilishi so'ralgan edi — oldingi uchta funksiya bilan bir xil sabab bilan mavjud TypeScript stekda amalga oshirildi; Redis ATAYLAB qo'shilmadi — flood/duplicate kuzatish oddiy Postgres jadval + vaqt oynasi so'rovi bilan yetarli, yangi infratuzilma bog'liqligi keraksiz. Guruh xabarlari BUTUNLAY alohida yo'lga (`handleGroupMessage`) yo'naltiriladi — chek/website-analyzer/kod-assistant/shaxsiy AI Assistant oqimlari faqat `ctx.chat.type === "private"` bo'lganda ishlaydi, guruh spamida tasodifan ishga tushib ketmasligi uchun. AI xatoga uchrasa (masalan kvota) xabar 'fail-open' tarzda BLOKLANMAYDI — soxta pozitiv (haqiqiy xabarlarni noto'g'ri o'chirish) risklidan ko'ra xavfsizroq. Jonli DB testda IKKITA haqiqiy xato topilib tuzatildi (tsc/eslint ushlamagan): (1) so'nggi xabarlar jadvaliga INSERT va shu jadvaldan SELECT `Promise.all` bilan PARALLEL bajarilgan edi — shu sabab hozirgi xabar SELECT natijasida ko'rinmay, flood/duplicate hisobi 1 birlikka kam chiqqan (flood/duplicate ATAYLAB ketma-ket INSERT-keyin-SELECT tarzida tuzatildi); (2) ogohlantirish sonini oshirish (`warnUser`) uchun noto'g'ri yozilgan `onConflictDoUpdate` ustunni solishtiruv ifodasiga (`eq(...)`) tenglashtirmoqchi bo'lgan, drizzle `sql` shablon operatori (`sql\`${col} + 1\``) bilan to'g'ri increment'ga almashtirildi. AI moderatsiya chaqiruvining o'zi (reklama/so'kinish/risk) shu kunda ham hali Gemini kvotasi tugagani sabab jonli sinalmadi — lekin bir xil `generateJson()` infratuzilmasidan foydalanadi, u website-analyzer va code-assistant'da allaqachon jonli tasdiqlangan.
- **2026-07-22** — Foydalanuvchi guruhga bot qo'shishni istamadi (avvalgi AI Group Moderation ishi guruh uchun edi) va o'rniga botni Telegram Business orqali O'ZINING shaxsiy akkountiga chatbot sifatida ishlatishni tanladi (bu allaqachon qisman mavjud edi — `bot.on("business_message")`). Shu asosda: (1) "Telefon" (PSTN/Telnyx) sahifasi va nav tab'i BUTUNLAY OLIB TASHLANDI (`src/app/(app)/telefon/*`, `src/lib/actions/personas.ts`) — foydalanuvchi aniq shunday so'radi ("telefon bolimini olib tashla"), chunki Telnyx hali ham "Pretrial" holatida qolib, foydalanuvchi bu yo'nalishni endi ustuvor deb hisoblamayapti. MUHIM: `ai_personas` jadvali va `users.activePersonaId`/`phoneAiEnabled` ustunlari ATAYLAB o'chirilmadi (faqat UI olib tashlandi) — chunki `src/app/api/telnyx/webhook/route.ts` hali ham shu ma'lumotga to'g'ridan-to'g'ri DB so'rovi orqali tayanadi (Telnyx kodi dormant, lekin ishlaydigan holatda qoldirilishi kerak edi). (2) O'rniga yangi `/shaxsiy-ai` sahifasi qo'shildi (`src/lib/actions/shaxsiy-ai.ts`) — Business ulanish holatini ko'rsatadi (mavjud `/telegram` sahifasidagi bilan bir xil `getBusinessConnectionStatus()`) va uchta yangi `users` ustuni (`business_voice_reply_enabled`, `business_link_analysis_enabled`, `business_video_download_enabled`) orqali uchta imkoniyatni alohida yoqish/o'chirish imkonini beradi. (3) `bot.on("business_message")` handleri sezilarli kengaytirildi: ovozli xabar kelsa — mavjud `transcribeAudio()`/`sendVoiceReply()` pipeline'i (2026-07-22'dagi voice-message qaroriga mos) qayta ishlatilib matn+ovoz javob beriladi; xabar faqat bitta havoladan iborat bo'lsa — mavjud website-analyzer (`extractSoleUrl`/`fetchSiteHtml`/`analyzeWebsite`) qayta ishlatiladi; xabar YouTube/Instagram havolasi bo'lsa — yangi `src/lib/video-downloader.ts` orqali video yuklab, `ctx.replyWithVideo()` bilan to'g'ridan-to'g'ri fayl sifatida yuboriladi. Uchala tekshiruv ham mos sozlama yoqilgan bo'lsagina ishlaydi va ANIQ shu tartibda (video -> link tahlili -> oddiy AI Assistant) bajariladi. MUHIM TOPILMA (jonli sinovda aniqlandi, hal qilinmadi): YouTube video yuklab olish uchun tanlangan `@distube/ytdl-core` kutubxonasi (pure JS, ffmpeg/Puppeteer'siz — loyiha konvensiyasiga mos) IKKITA ma'lum ochiq YouTube videosida ham "Sign in to confirm you're not a bot" xatosi bilan rad etildi — bu YouTube'ning joriy anti-bot himoyasi butun ytdl-core ekotizimiga qarshi ishlatilayotgani, kodning o'zida xato emas. Bu muhitdan (mahalliy tarmoq) test qilindi — Vercel'ning IP manzillaridan ishlashi yoki ishlamasligi HALI TEKSHIRILMAGAN. Instagram tomoni (sahifa HTML'idagi `og:video` meta tegini o'qish, rasmiy API emas) ham jonli sinalmadi. Shu sabab bu video-yuklab-berish qismi "eng yaxshi urinish" sifatida qoldirildi va foydalanuvchiga ochiq aytilgan — kelajakda YouTube tomoni butunlay ishlamay qolsa, muqobil yondashuv (masalan cookie-based autentifikatsiya yoki boshqa kutubxona) alohida ko'rib chiqilishi kerak bo'ladi.
- **2026-07-23** — Foydalanuvchi so'ragani bo'yicha **"Xarajat" bo'limi TO'LIQ olib tashlandi** (`/xarajat` sahifasi + nav tab, VA Telegram botning Paynet/PDF chekni avtomatik xarajat sifatida saqlash funksiyasi — foydalanuvchi ikkalasini ham xohlagani sabab, faqat sahifani emas). O'chirilgan fayllar: `src/app/(app)/xarajat/*`, `src/lib/actions/cards.ts`, `src/lib/actions/expenses.ts`, `src/lib/paynet-receipt.ts`, `src/lib/generic-receipt.ts`, `src/lib/expense-from-receipt.ts`, `src/lib/pdf-text.ts` (bularning barchasi endi hech qayerda ishlatilmagani tasdiqlangach o'chirildi — faqat bir-birlarini va endi yo'q xarajat oqimini import qilishardi); `telegram-bot.ts`dagi `handleReceiptText()`, `replySaveResult()` va butun `bot.on("message:document")` (PDF chek) handleri olib tashlandi; `next.config.ts`dagi `serverExternalPackages: ["unpdf"]` va `unpdf` npm bog'liqligi ham olib tashlandi (faqat shu PDF-chek funksiyasi uchun qo'shilgan edi). MUHIM: `cards`/`expenses` DB jadvallari va `cardBrand` enum ATAYLAB SAQLAB QOLINDI (schema.ts o'zgartirilmadi) — kod olib tashlash qaytariladigan (reversible) amal, lekin real ma'lumot (foydalanuvchi ilgari saqlagan xarajatlar) bo'lishi mumkin bo'lgan jadvalni DROP qilish qaytarib bo'lmaydigan amal, va foydalanuvchi buni aniq so'ramagan. O'chirishdan oldin "Tahlil" (AI kunlik/haftalik tahlil, `/tahlil`, `src/lib/ai-context.ts`) bu kodga hech qanday bog'liqligi yo'qligi tasdiqlandi (mustaqil), shuning uchun bu o'chirish boshqa hech narsaga ta'sir qilmadi.
